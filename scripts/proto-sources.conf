# Proto sources for code generation.
# One BSR module or git ref per line. Lines starting with # are ignored.
#
# ORDER MATTERS: xion must be LAST so its shared imports (cosmos_proto,
# tendermint, etc.) overwrite any older versions from earlier sources.
# The xion entry is overridable via XION_BSR_MODULE env var.

https://github.com/cosmos/ibc-apps.git#subdir=middleware/packet-forward-middleware/proto,branch=main
buf.build/cosmos/ibc
buf.build/cosmwasm/wasmd
buf.build/cosmos/cosmos-sdk
buf.build/burnt-labs/abstractaccount
buf.build/burnt-labs/tokenfactory
buf.build/burnt-labs/xion
