name: Release

# Called by upstream xion publish-release.yaml (workflow_call)
# or triggered manually. Generates all types, commits to a branch,
# creates a PR to main, auto-merges, and creates a GitHub release.

on:
  workflow_call:
    inputs:
      release_tag:
        description: "Xion release tag (e.g. v26.0.0)"
        required: true
        type: string
  workflow_dispatch:
    inputs:
      release_tag:
        description: "Xion release tag (e.g. v26.0.0)"
        required: true
        type: string

permissions:
  contents: write
  pull-requests: write

jobs:
  # ─── Stage 1: Generate all protobuf types ──────────────────────────
  generate:
    name: Generate
    uses: ./.github/workflows/generate-protobuf.yaml
    with:
      release_tag: ${{ inputs.release_tag }}
    secrets: inherit

  # ─── Stage 2: Commit, PR, merge, release ───────────────────────────
  commit-and-release:
    name: Commit & Release
    needs: [generate]
    runs-on: ubuntu-latest
    env:
      RELEASE_TAG: ${{ inputs.release_tag }}
      BRANCH: types/${{ inputs.release_tag }}
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GH_PAT }}
          fetch-depth: 0

      - name: Create types branch
        run: git checkout -b "$BRANCH"

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: generated-types-*
          merge-multiple: false

      - name: Organize artifacts
        run: bash scripts/ci/helper/organize-artifacts.sh

      - name: Commit generated types
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add -A
          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi

          git commit -m "chore: generate types for $RELEASE_TAG"
          git push -u origin "$BRANCH"

      - name: Create Pull Request
        id: pr
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
        run: |
          PR_URL=$(gh pr create \
            --base main \
            --head "$BRANCH" \
            --title "chore: update types for $RELEASE_TAG" \
            --body "Auto-generated protobuf types for xion [\`$RELEASE_TAG\`](https://github.com/burnt-labs/xion/releases/tag/$RELEASE_TAG)." \
            --label "automated")
          echo "pr_url=$PR_URL" >> "$GITHUB_OUTPUT"
          echo "✅ Created PR: $PR_URL"

      - name: Auto-merge Pull Request
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
        run: |
          gh pr merge "${{ steps.pr.outputs.pr_url }}" \
            --merge \
            --auto \
            --delete-branch
          echo "✅ PR set to auto-merge"

      - name: Wait for merge
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
        run: |
          PR_URL="${{ steps.pr.outputs.pr_url }}"
          for i in $(seq 1 30); do
            STATE=$(gh pr view "$PR_URL" --json state -q '.state')
            if [ "$STATE" = "MERGED" ]; then
              echo "✅ PR merged"
              exit 0
            fi
            echo "Waiting for merge... ($i/30, state=$STATE)"
            sleep 10
          done
          echo "❌ PR did not merge within 5 minutes"
          exit 1

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
        run: |
          # Pull latest main (contains the merge commit)
          git checkout main
          git pull origin main

          # Create release matching upstream xion tag
          gh release create "$RELEASE_TAG" \
            --title "$RELEASE_TAG" \
            --notes "Generated protobuf types for xion [\`$RELEASE_TAG\`](https://github.com/burnt-labs/xion/releases/tag/$RELEASE_TAG)." \
            --target main
          echo "✅ Created release $RELEASE_TAG"

  # ─── Summary ───────────────────────────────────────────────────────
  summary:
    name: Summary
    needs: [generate, commit-and-release]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Report
        run: |
          echo "## Release Summary for ${{ inputs.release_tag }}"
          echo ""
          echo "| Stage | Result |"
          echo "|-------|--------|"
          echo "| Generate | ${{ needs.generate.result }} |"
          echo "| Commit & Release | ${{ needs.commit-and-release.result }} |"

          if [ "${{ needs.generate.result }}" = "failure" ] || [ "${{ needs.commit-and-release.result }}" = "failure" ]; then
            echo ""
            echo "❌ Release pipeline failed"
            exit 1
          fi
          echo ""
          echo "✅ Release pipeline succeeded"
