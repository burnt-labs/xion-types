name: TypeScript Package CI/CD

on:
  push:
    branches: 
      # - main #TODO: Uncomment this when we want to publish to main
      - feat/do-99-package-releasing-automation
    paths:
      - 'ts/**'
      - 'proto/**'
      - '.github/workflows/typescript-ci.yaml'
  pull_request:
    branches: [main] #TODO : is it enough ?
    paths:
      - 'ts/**'
      - 'proto/**'
      - '.github/workflows/typescript-ci.yaml'
  workflow_dispatch:
    inputs:
      version_bump:
        description: 'Version bump type (patch, minor, major)'
        required: false
        default: 'patch'
        type: choice
        options:
          - patch
          - minor
          - major

env:
  NODE_VERSION: '18'
  REGISTRY_URL: 'https://registry.npmjs.org'

jobs:
  test:
    name: Publish Xion Types into NPM
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          submodules: false
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: 'ts/package-lock.json'

      - name: Install Dependencies
        working-directory: ./ts
        run: |
          # Try npm ci first, fallback to npm install if lock file is out of sync
          npm ci || npm install

      # - name: Run Tests
      #   working-directory: ./ts
      #   run: npm run test:ci

      - name: Build Package
        working-directory: ./ts
        run: npm run build

      - name: Verify Package Structure
        working-directory: ./ts
        run: |
          # Check if types directory exists and has content
          if [ ! -d "./types" ] || [ -z "$(ls -A ./types)" ]; then
            echo "Error: Types directory is empty or missing"
            echo "Contents of types directory:"
            ls -la types/ || echo "Types directory not found"
            exit 1
          fi
          
          # Count type files
          file_count=$(find ./types -name "*.ts" | wc -l)
          echo "Found $file_count TypeScript type files"
          
          # List some type files for verification
          echo "Sample type files:"
          find ./types -name "*.ts" | head -10

      - name: Type Check
        working-directory: ./ts
        run: |
          # Check if TypeScript is available and run type checking
          echo "Running TypeScript type check..."
          npx tsc --noEmit --skipLibCheck
          echo "Type check completed successfully"

  # publish:
  #   name: Publish to NPM
  #   needs: [test]
  #   runs-on: ubuntu-latest
  #   if: github.ref == 'refs/heads/main' && github.event_name == 'push'
  #   steps:
  #     - name: Checkout Repository
  #       uses: actions/checkout@v4
  #       with:
  #         submodules: false
  #         token: ${{ secrets.GITHUB_TOKEN }}

  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: ${{ env.NODE_VERSION }}
  #         registry-url: ${{ env.REGISTRY_URL }}

  #     - name: Install Dependencies
  #       working-directory: ./ts
  #       run: npm ci

  #     - name: Bump Version
  #       working-directory: ./ts
  #       run: |
  #         if [ "${{ github.event.inputs.version_bump }}" != "" ]; then
  #           npm version ${{ github.event.inputs.version_bump }} --no-git-tag-version
  #         else
  #           npm version patch --no-git-tag-version
  #         fi
        
  #         # Get the new version
  #         NEW_VERSION=$(node -p "require('./package.json').version")
  #         echo "NEW_VERSION=$NEW_VERSION" >> $GITHUB_ENV
  #         echo "New package version: $NEW_VERSION"

  #     - name: Prepare Package for Publishing
  #       working-directory: ./ts
  #       run: |
  #         # Ensure package structure is correct
  #         echo "Package structure:"
  #         ls -la
  #         echo "Types directory contents:"
  #         ls -la types/ | head -10

  #     - name: Publish to NPM
  #       working-directory: ./ts
  #       env:
  #         NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
  #       run: |
  #         echo "Publishing package version: ${{ env.NEW_VERSION }}"
  #         npm publish --access public
  #         echo "Successfully published @burnt-labs/xion-types@${{ env.NEW_VERSION }}"

  #     - name: Create Release
  #       uses: actions/create-release@v1
  #       env:
  #         GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  #       with:
  #         tag_name: v${{ env.NEW_VERSION }}
  #         release_name: Release v${{ env.NEW_VERSION }}
  #         body: |
  #           ## What's Changed
            
  #           This release includes:
  #           - TypeScript type definitions for Xion blockchain protocol
  #           - Cosmos SDK and IBC type definitions
            
  #           ## Installation
            
  #           ```bash
  #           npm install @burnt-labs/xion-types@${{ env.NEW_VERSION }}
  #           ```
            
  #           ## Usage
            
  #           ```typescript
  #           import { MsgSend } from '@burnt-labs/xion-types/cosmos/bank/v1beta1/tx';
  #           ```
  #         draft: false
  #         prerelease: false

  # validate-release:
  #   name: Validate Release
  #   needs: publish
  #   runs-on: ubuntu-latest
  #   if: github.ref == 'refs/heads/main' && github.event_name == 'push'
  #   steps:
  #     - name: Wait for NPM
  #       run: sleep 30

  #     - name: Verify Package on NPM
  #       run: |
  #         PACKAGE_VERSION="${{ needs.publish.outputs.NEW_VERSION }}"
  #         echo "Verifying package version: $PACKAGE_VERSION"
        
  #         # Check if package is available on NPM
  #         npm view @burnt-labs/xion-types@$PACKAGE_VERSION version
        
  #         echo "✅ Package successfully published to NPM"


