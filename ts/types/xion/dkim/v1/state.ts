// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: xion/dkim/v1/state.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "xion.dkim.v1";

/** Version defines the supported DKIM versions. */
export enum Version {
  /** VERSION_DKIM1_UNSPECIFIED - VERSION_DKIM1_UNSPECIFIED represents DKIM version 1 (unspecified default). */
  VERSION_DKIM1_UNSPECIFIED = 0,
  UNRECOGNIZED = -1,
}

export function versionFromJSON(object: any): Version {
  switch (object) {
    case 0:
    case "VERSION_DKIM1_UNSPECIFIED":
      return Version.VERSION_DKIM1_UNSPECIFIED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return Version.UNRECOGNIZED;
  }
}

export function versionToJSON(object: Version): string {
  switch (object) {
    case Version.VERSION_DKIM1_UNSPECIFIED:
      return "VERSION_DKIM1_UNSPECIFIED";
    case Version.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** KeyType defines the supported cryptographic key types for DKIM. */
export enum KeyType {
  /** KEY_TYPE_RSA_UNSPECIFIED - KEY_TYPE_RSA_UNSPECIFIED represents RSA key type (unspecified default). */
  KEY_TYPE_RSA_UNSPECIFIED = 0,
  UNRECOGNIZED = -1,
}

export function keyTypeFromJSON(object: any): KeyType {
  switch (object) {
    case 0:
    case "KEY_TYPE_RSA_UNSPECIFIED":
      return KeyType.KEY_TYPE_RSA_UNSPECIFIED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return KeyType.UNRECOGNIZED;
  }
}

export function keyTypeToJSON(object: KeyType): string {
  switch (object) {
    case KeyType.KEY_TYPE_RSA_UNSPECIFIED:
      return "KEY_TYPE_RSA_UNSPECIFIED";
    case KeyType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** DkimPubKey represents a DKIM public key with associated metadata. */
export interface DkimPubKey {
  /** domain defines the email domain associated with this DKIM key. */
  domain: string;
  /** pub_key defines the base64 encoded public key. */
  pubKey: string;
  /** poseidon_hash defines the Poseidon hash of the public key. */
  poseidonHash: Uint8Array;
  /** selector defines the DKIM selector for this key. */
  selector: string;
  /** version defines the DKIM version. */
  version: Version;
  /** key_type defines the cryptographic key type. */
  keyType: KeyType;
}

function createBaseDkimPubKey(): DkimPubKey {
  return { domain: "", pubKey: "", poseidonHash: new Uint8Array(0), selector: "", version: 0, keyType: 0 };
}

export const DkimPubKey: MessageFns<DkimPubKey> = {
  encode(message: DkimPubKey, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.domain !== "") {
      writer.uint32(10).string(message.domain);
    }
    if (message.pubKey !== "") {
      writer.uint32(18).string(message.pubKey);
    }
    if (message.poseidonHash.length !== 0) {
      writer.uint32(26).bytes(message.poseidonHash);
    }
    if (message.selector !== "") {
      writer.uint32(34).string(message.selector);
    }
    if (message.version !== 0) {
      writer.uint32(40).int32(message.version);
    }
    if (message.keyType !== 0) {
      writer.uint32(48).int32(message.keyType);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DkimPubKey {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDkimPubKey();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.domain = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.pubKey = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.poseidonHash = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.selector = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.version = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.keyType = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DkimPubKey {
    return {
      domain: isSet(object.domain) ? globalThis.String(object.domain) : "",
      pubKey: isSet(object.pubKey)
        ? globalThis.String(object.pubKey)
        : isSet(object.pub_key)
        ? globalThis.String(object.pub_key)
        : "",
      poseidonHash: isSet(object.poseidonHash)
        ? bytesFromBase64(object.poseidonHash)
        : isSet(object.poseidon_hash)
        ? bytesFromBase64(object.poseidon_hash)
        : new Uint8Array(0),
      selector: isSet(object.selector) ? globalThis.String(object.selector) : "",
      version: isSet(object.version) ? versionFromJSON(object.version) : 0,
      keyType: isSet(object.keyType)
        ? keyTypeFromJSON(object.keyType)
        : isSet(object.key_type)
        ? keyTypeFromJSON(object.key_type)
        : 0,
    };
  },

  toJSON(message: DkimPubKey): unknown {
    const obj: any = {};
    if (message.domain !== "") {
      obj.domain = message.domain;
    }
    if (message.pubKey !== "") {
      obj.pubKey = message.pubKey;
    }
    if (message.poseidonHash.length !== 0) {
      obj.poseidonHash = base64FromBytes(message.poseidonHash);
    }
    if (message.selector !== "") {
      obj.selector = message.selector;
    }
    if (message.version !== 0) {
      obj.version = versionToJSON(message.version);
    }
    if (message.keyType !== 0) {
      obj.keyType = keyTypeToJSON(message.keyType);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DkimPubKey>, I>>(base?: I): DkimPubKey {
    return DkimPubKey.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DkimPubKey>, I>>(object: I): DkimPubKey {
    const message = createBaseDkimPubKey();
    message.domain = object.domain ?? "";
    message.pubKey = object.pubKey ?? "";
    message.poseidonHash = object.poseidonHash ?? new Uint8Array(0);
    message.selector = object.selector ?? "";
    message.version = object.version ?? 0;
    message.keyType = object.keyType ?? 0;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
