// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: xion/dkim/v1/query.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { grpc } from "@improbable-eng/grpc-web";
import { BrowserHeaders } from "browser-headers";
import { PageRequest, PageResponse } from "../../../cosmos/base/query/v1beta1/pagination";
import { Params } from "./genesis";
import { DkimPubKey } from "./state";

export const protobufPackage = "xion.dkim.v1";

/**
 * QueryDkimPubKeyRequest is the request type for the Query/DkimPubKey RPC
 * method.
 */
export interface QueryDkimPubKeyRequest {
  /** selector defines the DKIM selector to query. */
  selector: string;
  /** domain defines the domain to query. */
  domain: string;
}

/**
 * QueryDkimPubKeyResponse is the response type for the Query/DkimPubKey RPC
 * method.
 */
export interface QueryDkimPubKeyResponse {
  /** dkim_pub_key defines the DKIM public key. */
  dkimPubKey?: DkimPubKey | undefined;
}

/**
 * QueryDkimPubKeysRequest is the request type for the Query/DkimPubKeys RPC
 * method. All fields are optional, and will filter down results.
 */
export interface QueryDkimPubKeysRequest {
  /** selector defines the DKIM selector to filter by. */
  selector: string;
  /** domain defines the domain to filter by. */
  domain: string;
  /** poseidon_hash defines the Poseidon hash to filter by. */
  poseidonHash: Uint8Array;
  /** pagination defines the pagination parameters. */
  pagination?: PageRequest | undefined;
}

/**
 * QueryDkimPubKeysResponse is the response type for the Query/DkimPubKeys RPC
 * method.
 */
export interface QueryDkimPubKeysResponse {
  /** dkim_pub_keys defines the list of DKIM public keys. */
  dkimPubKeys: DkimPubKey[];
  /** pagination defines the pagination response. */
  pagination?: PageResponse | undefined;
}

/**
 * QueryAuthenticateRequest defines the request structure for proof
 * verification.
 */
export interface QueryAuthenticateRequest {
  /** tx_bytes defines the serialized transaction bytes. */
  txBytes: Uint8Array;
  /** email_hash defines the hash of the email being authenticated. */
  emailHash: string;
  /** proof defines the zk proof bytes. */
  proof: Uint8Array;
  /** public_inputs defines the public inputs for the zk proof. */
  publicInputs: string[];
  /** allowed_email_hosts defines the list of allowed email hosts. */
  allowedEmailHosts: string[];
}

/** AuthenticateResponse defines the response structure for proof verification. */
export interface AuthenticateResponse {
  /** verified indicates whether the proof verification was successful. */
  verified: boolean;
}

/** QueryParamsRequest is the request type for the Query/Params RPC method. */
export interface QueryParamsRequest {
}

/** QueryParamsResponse is the response type for the Query/Params RPC method. */
export interface QueryParamsResponse {
  /** params defines the parameters of the module. */
  params?: Params | undefined;
}

function createBaseQueryDkimPubKeyRequest(): QueryDkimPubKeyRequest {
  return { selector: "", domain: "" };
}

export const QueryDkimPubKeyRequest: MessageFns<QueryDkimPubKeyRequest> = {
  encode(message: QueryDkimPubKeyRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.selector !== "") {
      writer.uint32(10).string(message.selector);
    }
    if (message.domain !== "") {
      writer.uint32(18).string(message.domain);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryDkimPubKeyRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryDkimPubKeyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.selector = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.domain = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryDkimPubKeyRequest {
    return {
      selector: isSet(object.selector) ? globalThis.String(object.selector) : "",
      domain: isSet(object.domain) ? globalThis.String(object.domain) : "",
    };
  },

  toJSON(message: QueryDkimPubKeyRequest): unknown {
    const obj: any = {};
    if (message.selector !== "") {
      obj.selector = message.selector;
    }
    if (message.domain !== "") {
      obj.domain = message.domain;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryDkimPubKeyRequest>, I>>(base?: I): QueryDkimPubKeyRequest {
    return QueryDkimPubKeyRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryDkimPubKeyRequest>, I>>(object: I): QueryDkimPubKeyRequest {
    const message = createBaseQueryDkimPubKeyRequest();
    message.selector = object.selector ?? "";
    message.domain = object.domain ?? "";
    return message;
  },
};

function createBaseQueryDkimPubKeyResponse(): QueryDkimPubKeyResponse {
  return { dkimPubKey: undefined };
}

export const QueryDkimPubKeyResponse: MessageFns<QueryDkimPubKeyResponse> = {
  encode(message: QueryDkimPubKeyResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.dkimPubKey !== undefined) {
      DkimPubKey.encode(message.dkimPubKey, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryDkimPubKeyResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryDkimPubKeyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.dkimPubKey = DkimPubKey.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryDkimPubKeyResponse {
    return {
      dkimPubKey: isSet(object.dkimPubKey)
        ? DkimPubKey.fromJSON(object.dkimPubKey)
        : isSet(object.dkim_pub_key)
        ? DkimPubKey.fromJSON(object.dkim_pub_key)
        : undefined,
    };
  },

  toJSON(message: QueryDkimPubKeyResponse): unknown {
    const obj: any = {};
    if (message.dkimPubKey !== undefined) {
      obj.dkimPubKey = DkimPubKey.toJSON(message.dkimPubKey);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryDkimPubKeyResponse>, I>>(base?: I): QueryDkimPubKeyResponse {
    return QueryDkimPubKeyResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryDkimPubKeyResponse>, I>>(object: I): QueryDkimPubKeyResponse {
    const message = createBaseQueryDkimPubKeyResponse();
    message.dkimPubKey = (object.dkimPubKey !== undefined && object.dkimPubKey !== null)
      ? DkimPubKey.fromPartial(object.dkimPubKey)
      : undefined;
    return message;
  },
};

function createBaseQueryDkimPubKeysRequest(): QueryDkimPubKeysRequest {
  return { selector: "", domain: "", poseidonHash: new Uint8Array(0), pagination: undefined };
}

export const QueryDkimPubKeysRequest: MessageFns<QueryDkimPubKeysRequest> = {
  encode(message: QueryDkimPubKeysRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.selector !== "") {
      writer.uint32(10).string(message.selector);
    }
    if (message.domain !== "") {
      writer.uint32(18).string(message.domain);
    }
    if (message.poseidonHash.length !== 0) {
      writer.uint32(26).bytes(message.poseidonHash);
    }
    if (message.pagination !== undefined) {
      PageRequest.encode(message.pagination, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryDkimPubKeysRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryDkimPubKeysRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.selector = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.domain = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.poseidonHash = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.pagination = PageRequest.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryDkimPubKeysRequest {
    return {
      selector: isSet(object.selector) ? globalThis.String(object.selector) : "",
      domain: isSet(object.domain) ? globalThis.String(object.domain) : "",
      poseidonHash: isSet(object.poseidonHash)
        ? bytesFromBase64(object.poseidonHash)
        : isSet(object.poseidon_hash)
        ? bytesFromBase64(object.poseidon_hash)
        : new Uint8Array(0),
      pagination: isSet(object.pagination) ? PageRequest.fromJSON(object.pagination) : undefined,
    };
  },

  toJSON(message: QueryDkimPubKeysRequest): unknown {
    const obj: any = {};
    if (message.selector !== "") {
      obj.selector = message.selector;
    }
    if (message.domain !== "") {
      obj.domain = message.domain;
    }
    if (message.poseidonHash.length !== 0) {
      obj.poseidonHash = base64FromBytes(message.poseidonHash);
    }
    if (message.pagination !== undefined) {
      obj.pagination = PageRequest.toJSON(message.pagination);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryDkimPubKeysRequest>, I>>(base?: I): QueryDkimPubKeysRequest {
    return QueryDkimPubKeysRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryDkimPubKeysRequest>, I>>(object: I): QueryDkimPubKeysRequest {
    const message = createBaseQueryDkimPubKeysRequest();
    message.selector = object.selector ?? "";
    message.domain = object.domain ?? "";
    message.poseidonHash = object.poseidonHash ?? new Uint8Array(0);
    message.pagination = (object.pagination !== undefined && object.pagination !== null)
      ? PageRequest.fromPartial(object.pagination)
      : undefined;
    return message;
  },
};

function createBaseQueryDkimPubKeysResponse(): QueryDkimPubKeysResponse {
  return { dkimPubKeys: [], pagination: undefined };
}

export const QueryDkimPubKeysResponse: MessageFns<QueryDkimPubKeysResponse> = {
  encode(message: QueryDkimPubKeysResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.dkimPubKeys) {
      DkimPubKey.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.pagination !== undefined) {
      PageResponse.encode(message.pagination, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryDkimPubKeysResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryDkimPubKeysResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.dkimPubKeys.push(DkimPubKey.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.pagination = PageResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryDkimPubKeysResponse {
    return {
      dkimPubKeys: globalThis.Array.isArray(object?.dkimPubKeys)
        ? object.dkimPubKeys.map((e: any) => DkimPubKey.fromJSON(e))
        : globalThis.Array.isArray(object?.dkim_pub_keys)
        ? object.dkim_pub_keys.map((e: any) => DkimPubKey.fromJSON(e))
        : [],
      pagination: isSet(object.pagination) ? PageResponse.fromJSON(object.pagination) : undefined,
    };
  },

  toJSON(message: QueryDkimPubKeysResponse): unknown {
    const obj: any = {};
    if (message.dkimPubKeys?.length) {
      obj.dkimPubKeys = message.dkimPubKeys.map((e) => DkimPubKey.toJSON(e));
    }
    if (message.pagination !== undefined) {
      obj.pagination = PageResponse.toJSON(message.pagination);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryDkimPubKeysResponse>, I>>(base?: I): QueryDkimPubKeysResponse {
    return QueryDkimPubKeysResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryDkimPubKeysResponse>, I>>(object: I): QueryDkimPubKeysResponse {
    const message = createBaseQueryDkimPubKeysResponse();
    message.dkimPubKeys = object.dkimPubKeys?.map((e) => DkimPubKey.fromPartial(e)) || [];
    message.pagination = (object.pagination !== undefined && object.pagination !== null)
      ? PageResponse.fromPartial(object.pagination)
      : undefined;
    return message;
  },
};

function createBaseQueryAuthenticateRequest(): QueryAuthenticateRequest {
  return {
    txBytes: new Uint8Array(0),
    emailHash: "",
    proof: new Uint8Array(0),
    publicInputs: [],
    allowedEmailHosts: [],
  };
}

export const QueryAuthenticateRequest: MessageFns<QueryAuthenticateRequest> = {
  encode(message: QueryAuthenticateRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.txBytes.length !== 0) {
      writer.uint32(10).bytes(message.txBytes);
    }
    if (message.emailHash !== "") {
      writer.uint32(18).string(message.emailHash);
    }
    if (message.proof.length !== 0) {
      writer.uint32(26).bytes(message.proof);
    }
    for (const v of message.publicInputs) {
      writer.uint32(34).string(v!);
    }
    for (const v of message.allowedEmailHosts) {
      writer.uint32(42).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryAuthenticateRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryAuthenticateRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.txBytes = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.emailHash = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.proof = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.publicInputs.push(reader.string());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.allowedEmailHosts.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryAuthenticateRequest {
    return {
      txBytes: isSet(object.txBytes)
        ? bytesFromBase64(object.txBytes)
        : isSet(object.tx_bytes)
        ? bytesFromBase64(object.tx_bytes)
        : new Uint8Array(0),
      emailHash: isSet(object.emailHash)
        ? globalThis.String(object.emailHash)
        : isSet(object.email_hash)
        ? globalThis.String(object.email_hash)
        : "",
      proof: isSet(object.proof) ? bytesFromBase64(object.proof) : new Uint8Array(0),
      publicInputs: globalThis.Array.isArray(object?.publicInputs)
        ? object.publicInputs.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.public_inputs)
        ? object.public_inputs.map((e: any) => globalThis.String(e))
        : [],
      allowedEmailHosts: globalThis.Array.isArray(object?.allowedEmailHosts)
        ? object.allowedEmailHosts.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.allowed_email_hosts)
        ? object.allowed_email_hosts.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: QueryAuthenticateRequest): unknown {
    const obj: any = {};
    if (message.txBytes.length !== 0) {
      obj.txBytes = base64FromBytes(message.txBytes);
    }
    if (message.emailHash !== "") {
      obj.emailHash = message.emailHash;
    }
    if (message.proof.length !== 0) {
      obj.proof = base64FromBytes(message.proof);
    }
    if (message.publicInputs?.length) {
      obj.publicInputs = message.publicInputs;
    }
    if (message.allowedEmailHosts?.length) {
      obj.allowedEmailHosts = message.allowedEmailHosts;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryAuthenticateRequest>, I>>(base?: I): QueryAuthenticateRequest {
    return QueryAuthenticateRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryAuthenticateRequest>, I>>(object: I): QueryAuthenticateRequest {
    const message = createBaseQueryAuthenticateRequest();
    message.txBytes = object.txBytes ?? new Uint8Array(0);
    message.emailHash = object.emailHash ?? "";
    message.proof = object.proof ?? new Uint8Array(0);
    message.publicInputs = object.publicInputs?.map((e) => e) || [];
    message.allowedEmailHosts = object.allowedEmailHosts?.map((e) => e) || [];
    return message;
  },
};

function createBaseAuthenticateResponse(): AuthenticateResponse {
  return { verified: false };
}

export const AuthenticateResponse: MessageFns<AuthenticateResponse> = {
  encode(message: AuthenticateResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.verified !== false) {
      writer.uint32(8).bool(message.verified);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AuthenticateResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuthenticateResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.verified = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AuthenticateResponse {
    return { verified: isSet(object.verified) ? globalThis.Boolean(object.verified) : false };
  },

  toJSON(message: AuthenticateResponse): unknown {
    const obj: any = {};
    if (message.verified !== false) {
      obj.verified = message.verified;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AuthenticateResponse>, I>>(base?: I): AuthenticateResponse {
    return AuthenticateResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AuthenticateResponse>, I>>(object: I): AuthenticateResponse {
    const message = createBaseAuthenticateResponse();
    message.verified = object.verified ?? false;
    return message;
  },
};

function createBaseQueryParamsRequest(): QueryParamsRequest {
  return {};
}

export const QueryParamsRequest: MessageFns<QueryParamsRequest> = {
  encode(_: QueryParamsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryParamsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryParamsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): QueryParamsRequest {
    return {};
  },

  toJSON(_: QueryParamsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryParamsRequest>, I>>(base?: I): QueryParamsRequest {
    return QueryParamsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryParamsRequest>, I>>(_: I): QueryParamsRequest {
    const message = createBaseQueryParamsRequest();
    return message;
  },
};

function createBaseQueryParamsResponse(): QueryParamsResponse {
  return { params: undefined };
}

export const QueryParamsResponse: MessageFns<QueryParamsResponse> = {
  encode(message: QueryParamsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryParamsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryParamsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.params = Params.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryParamsResponse {
    return { params: isSet(object.params) ? Params.fromJSON(object.params) : undefined };
  },

  toJSON(message: QueryParamsResponse): unknown {
    const obj: any = {};
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryParamsResponse>, I>>(base?: I): QueryParamsResponse {
    return QueryParamsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryParamsResponse>, I>>(object: I): QueryParamsResponse {
    const message = createBaseQueryParamsResponse();
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    return message;
  },
};

/** Query provides defines the gRPC querier service. */
export interface Query {
  /** DkimPubKey queries the DKIM public key for a given selector and domain. */
  DkimPubKey(request: DeepPartial<QueryDkimPubKeyRequest>, metadata?: grpc.Metadata): Promise<QueryDkimPubKeyResponse>;
  /** DkimPubKeys queries the DKIM public keys for a given selectors and domains. */
  DkimPubKeys(
    request: DeepPartial<QueryDkimPubKeysRequest>,
    metadata?: grpc.Metadata,
  ): Promise<QueryDkimPubKeysResponse>;
  /** Authenticate verifies a zk proof for email authentication. */
  Authenticate(request: DeepPartial<QueryAuthenticateRequest>, metadata?: grpc.Metadata): Promise<AuthenticateResponse>;
  /** Params queries all parameters of the module. */
  Params(request: DeepPartial<QueryParamsRequest>, metadata?: grpc.Metadata): Promise<QueryParamsResponse>;
}

export class QueryClientImpl implements Query {
  private readonly rpc: Rpc;

  constructor(rpc: Rpc) {
    this.rpc = rpc;
    this.DkimPubKey = this.DkimPubKey.bind(this);
    this.DkimPubKeys = this.DkimPubKeys.bind(this);
    this.Authenticate = this.Authenticate.bind(this);
    this.Params = this.Params.bind(this);
  }

  DkimPubKey(request: DeepPartial<QueryDkimPubKeyRequest>, metadata?: grpc.Metadata): Promise<QueryDkimPubKeyResponse> {
    return this.rpc.unary(QueryDkimPubKeyDesc, QueryDkimPubKeyRequest.fromPartial(request), metadata);
  }

  DkimPubKeys(
    request: DeepPartial<QueryDkimPubKeysRequest>,
    metadata?: grpc.Metadata,
  ): Promise<QueryDkimPubKeysResponse> {
    return this.rpc.unary(QueryDkimPubKeysDesc, QueryDkimPubKeysRequest.fromPartial(request), metadata);
  }

  Authenticate(
    request: DeepPartial<QueryAuthenticateRequest>,
    metadata?: grpc.Metadata,
  ): Promise<AuthenticateResponse> {
    return this.rpc.unary(QueryAuthenticateDesc, QueryAuthenticateRequest.fromPartial(request), metadata);
  }

  Params(request: DeepPartial<QueryParamsRequest>, metadata?: grpc.Metadata): Promise<QueryParamsResponse> {
    return this.rpc.unary(QueryParamsDesc, QueryParamsRequest.fromPartial(request), metadata);
  }
}

export const QueryDesc = { serviceName: "xion.dkim.v1.Query" };

export const QueryDkimPubKeyDesc: UnaryMethodDefinitionish = {
  methodName: "DkimPubKey",
  service: QueryDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return QueryDkimPubKeyRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = QueryDkimPubKeyResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const QueryDkimPubKeysDesc: UnaryMethodDefinitionish = {
  methodName: "DkimPubKeys",
  service: QueryDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return QueryDkimPubKeysRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = QueryDkimPubKeysResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const QueryAuthenticateDesc: UnaryMethodDefinitionish = {
  methodName: "Authenticate",
  service: QueryDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return QueryAuthenticateRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = AuthenticateResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const QueryParamsDesc: UnaryMethodDefinitionish = {
  methodName: "Params",
  service: QueryDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return QueryParamsRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = QueryParamsResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export interface UnaryMethodDefinitionishR extends grpc.UnaryMethodDefinition<any, any> {
  requestStream: any;
  responseStream: any;
}

type UnaryMethodDefinitionish = UnaryMethodDefinitionishR;

interface Rpc {
  unary<T extends UnaryMethodDefinitionish>(
    methodDesc: T,
    request: any,
    metadata: grpc.Metadata | undefined,
  ): Promise<any>;
}

export class GrpcWebImpl {
  private host: string;
  private options: {
    transport?: grpc.TransportFactory;

    debug?: boolean;
    metadata?: grpc.Metadata;
    upStreamRetryCodes?: number[];
  };

  constructor(
    host: string,
    options: {
      transport?: grpc.TransportFactory;

      debug?: boolean;
      metadata?: grpc.Metadata;
      upStreamRetryCodes?: number[];
    },
  ) {
    this.host = host;
    this.options = options;
  }

  unary<T extends UnaryMethodDefinitionish>(
    methodDesc: T,
    _request: any,
    metadata: grpc.Metadata | undefined,
  ): Promise<any> {
    const request = { ..._request, ...methodDesc.requestType };
    const maybeCombinedMetadata = metadata && this.options.metadata
      ? new BrowserHeaders({ ...this.options?.metadata.headersMap, ...metadata?.headersMap })
      : metadata ?? this.options.metadata;
    return new Promise((resolve, reject) => {
      grpc.unary(methodDesc, {
        request,
        host: this.host,
        metadata: maybeCombinedMetadata ?? {},
        ...(this.options.transport !== undefined ? { transport: this.options.transport } : {}),
        debug: this.options.debug ?? false,
        onEnd: function (response) {
          if (response.status === grpc.Code.OK) {
            resolve(response.message!.toObject());
          } else {
            const err = new GrpcWebError(response.statusMessage, response.status, response.trailers);
            reject(err);
          }
        },
      });
    });
  }
}

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export class GrpcWebError extends globalThis.Error {
  constructor(message: string, public code: grpc.Code, public metadata: grpc.Metadata) {
    super(message);
  }
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
