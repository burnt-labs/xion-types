// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               unknown
// source: xion/dkim/v1/tx.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { grpc } from "@improbable-eng/grpc-web";
import { BrowserHeaders } from "browser-headers";
import Long from "long";
import { Params } from "./genesis";
import { DkimPubKey } from "./state";

export const protobufPackage = "xion.dkim.v1";

/**
 * MsgUpdateParamsResponse defines the response structure for executing a
 * MsgUpdateParams message.
 *
 * Since: cosmos-sdk 0.47
 */
export interface MsgUpdateParamsResponse {
}

/**
 * MsgUpdateParams is the Msg/UpdateParams request type.
 *
 * Since: cosmos-sdk 0.47
 */
export interface MsgUpdateParams {
  /** authority is the address of the governance account. */
  authority: string;
  /**
   * params defines the parameters to update.
   *
   * NOTE: All parameters must be supplied.
   */
  params?: Params | undefined;
}

/** MsgAddDkimPubKey is the Msg/AddDkimPubKey request type. */
export interface MsgAddDkimPubKeys {
  /** authority is the address of the governance account. */
  authority: string;
  /** dkim_pubkeys defines the DKIM public keys to add. */
  dkimPubkeys: DkimPubKey[];
}

/**
 * MsgAddDkimPubKeyResponse defines the response structure for executing a
 * MsgAddDkimPubKey message.
 */
export interface MsgAddDkimPubKeysResponse {
}

/** MsgRemoveDkimPubKey is the Msg/RemoveDkimPubKey request type. */
export interface MsgRemoveDkimPubKey {
  /** authority is the address of the governance account. */
  authority: string;
  /** selector defines the DKIM selector to remove. */
  selector: string;
  /** domain defines the domain for the DKIM key to remove. */
  domain: string;
}

/**
 * MsgRemoveDkimPubKeyResponse defines the response structure for executing a
 * MsgRemoveDkimPubKey message.
 */
export interface MsgRemoveDkimPubKeyResponse {
}

/** MsgRevokeDkimPubKey is the Msg/RevokeDkimPubKey request type. */
export interface MsgRevokeDkimPubKey {
  /** signer defines the address of the signer revoking the key. */
  signer: string;
  /** domain defines the domain for the DKIM key to revoke. */
  domain: string;
  /** priv_key defines the private key used to prove ownership for revocation. */
  privKey: Uint8Array;
}

/**
 * MsgRevokeDkimPubKeyResponse defines the response structure for executing a
 * MsgRevokeDkimPubKey message.
 */
export interface MsgRevokeDkimPubKeyResponse {
}

function createBaseMsgUpdateParamsResponse(): MsgUpdateParamsResponse {
  return {};
}

export const MsgUpdateParamsResponse: MessageFns<MsgUpdateParamsResponse> = {
  encode(_: MsgUpdateParamsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgUpdateParamsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgUpdateParamsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgUpdateParamsResponse {
    return {};
  },

  toJSON(_: MsgUpdateParamsResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgUpdateParamsResponse>, I>>(base?: I): MsgUpdateParamsResponse {
    return MsgUpdateParamsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgUpdateParamsResponse>, I>>(_: I): MsgUpdateParamsResponse {
    const message = createBaseMsgUpdateParamsResponse();
    return message;
  },
};

function createBaseMsgUpdateParams(): MsgUpdateParams {
  return { authority: "", params: undefined };
}

export const MsgUpdateParams: MessageFns<MsgUpdateParams> = {
  encode(message: MsgUpdateParams, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.authority !== "") {
      writer.uint32(10).string(message.authority);
    }
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgUpdateParams {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgUpdateParams();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.authority = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.params = Params.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgUpdateParams {
    return {
      authority: isSet(object.authority) ? globalThis.String(object.authority) : "",
      params: isSet(object.params) ? Params.fromJSON(object.params) : undefined,
    };
  },

  toJSON(message: MsgUpdateParams): unknown {
    const obj: any = {};
    if (message.authority !== "") {
      obj.authority = message.authority;
    }
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgUpdateParams>, I>>(base?: I): MsgUpdateParams {
    return MsgUpdateParams.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgUpdateParams>, I>>(object: I): MsgUpdateParams {
    const message = createBaseMsgUpdateParams();
    message.authority = object.authority ?? "";
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    return message;
  },
};

function createBaseMsgAddDkimPubKeys(): MsgAddDkimPubKeys {
  return { authority: "", dkimPubkeys: [] };
}

export const MsgAddDkimPubKeys: MessageFns<MsgAddDkimPubKeys> = {
  encode(message: MsgAddDkimPubKeys, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.authority !== "") {
      writer.uint32(10).string(message.authority);
    }
    for (const v of message.dkimPubkeys) {
      DkimPubKey.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgAddDkimPubKeys {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgAddDkimPubKeys();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.authority = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.dkimPubkeys.push(DkimPubKey.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgAddDkimPubKeys {
    return {
      authority: isSet(object.authority) ? globalThis.String(object.authority) : "",
      dkimPubkeys: globalThis.Array.isArray(object?.dkimPubkeys)
        ? object.dkimPubkeys.map((e: any) => DkimPubKey.fromJSON(e))
        : globalThis.Array.isArray(object?.dkim_pubkeys)
        ? object.dkim_pubkeys.map((e: any) => DkimPubKey.fromJSON(e))
        : [],
    };
  },

  toJSON(message: MsgAddDkimPubKeys): unknown {
    const obj: any = {};
    if (message.authority !== "") {
      obj.authority = message.authority;
    }
    if (message.dkimPubkeys?.length) {
      obj.dkimPubkeys = message.dkimPubkeys.map((e) => DkimPubKey.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgAddDkimPubKeys>, I>>(base?: I): MsgAddDkimPubKeys {
    return MsgAddDkimPubKeys.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgAddDkimPubKeys>, I>>(object: I): MsgAddDkimPubKeys {
    const message = createBaseMsgAddDkimPubKeys();
    message.authority = object.authority ?? "";
    message.dkimPubkeys = object.dkimPubkeys?.map((e) => DkimPubKey.fromPartial(e)) || [];
    return message;
  },
};

function createBaseMsgAddDkimPubKeysResponse(): MsgAddDkimPubKeysResponse {
  return {};
}

export const MsgAddDkimPubKeysResponse: MessageFns<MsgAddDkimPubKeysResponse> = {
  encode(_: MsgAddDkimPubKeysResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgAddDkimPubKeysResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgAddDkimPubKeysResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgAddDkimPubKeysResponse {
    return {};
  },

  toJSON(_: MsgAddDkimPubKeysResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgAddDkimPubKeysResponse>, I>>(base?: I): MsgAddDkimPubKeysResponse {
    return MsgAddDkimPubKeysResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgAddDkimPubKeysResponse>, I>>(_: I): MsgAddDkimPubKeysResponse {
    const message = createBaseMsgAddDkimPubKeysResponse();
    return message;
  },
};

function createBaseMsgRemoveDkimPubKey(): MsgRemoveDkimPubKey {
  return { authority: "", selector: "", domain: "" };
}

export const MsgRemoveDkimPubKey: MessageFns<MsgRemoveDkimPubKey> = {
  encode(message: MsgRemoveDkimPubKey, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.authority !== "") {
      writer.uint32(10).string(message.authority);
    }
    if (message.selector !== "") {
      writer.uint32(18).string(message.selector);
    }
    if (message.domain !== "") {
      writer.uint32(26).string(message.domain);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgRemoveDkimPubKey {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRemoveDkimPubKey();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.authority = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.selector = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.domain = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgRemoveDkimPubKey {
    return {
      authority: isSet(object.authority) ? globalThis.String(object.authority) : "",
      selector: isSet(object.selector) ? globalThis.String(object.selector) : "",
      domain: isSet(object.domain) ? globalThis.String(object.domain) : "",
    };
  },

  toJSON(message: MsgRemoveDkimPubKey): unknown {
    const obj: any = {};
    if (message.authority !== "") {
      obj.authority = message.authority;
    }
    if (message.selector !== "") {
      obj.selector = message.selector;
    }
    if (message.domain !== "") {
      obj.domain = message.domain;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgRemoveDkimPubKey>, I>>(base?: I): MsgRemoveDkimPubKey {
    return MsgRemoveDkimPubKey.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgRemoveDkimPubKey>, I>>(object: I): MsgRemoveDkimPubKey {
    const message = createBaseMsgRemoveDkimPubKey();
    message.authority = object.authority ?? "";
    message.selector = object.selector ?? "";
    message.domain = object.domain ?? "";
    return message;
  },
};

function createBaseMsgRemoveDkimPubKeyResponse(): MsgRemoveDkimPubKeyResponse {
  return {};
}

export const MsgRemoveDkimPubKeyResponse: MessageFns<MsgRemoveDkimPubKeyResponse> = {
  encode(_: MsgRemoveDkimPubKeyResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgRemoveDkimPubKeyResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRemoveDkimPubKeyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgRemoveDkimPubKeyResponse {
    return {};
  },

  toJSON(_: MsgRemoveDkimPubKeyResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgRemoveDkimPubKeyResponse>, I>>(base?: I): MsgRemoveDkimPubKeyResponse {
    return MsgRemoveDkimPubKeyResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgRemoveDkimPubKeyResponse>, I>>(_: I): MsgRemoveDkimPubKeyResponse {
    const message = createBaseMsgRemoveDkimPubKeyResponse();
    return message;
  },
};

function createBaseMsgRevokeDkimPubKey(): MsgRevokeDkimPubKey {
  return { signer: "", domain: "", privKey: new Uint8Array(0) };
}

export const MsgRevokeDkimPubKey: MessageFns<MsgRevokeDkimPubKey> = {
  encode(message: MsgRevokeDkimPubKey, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.signer !== "") {
      writer.uint32(10).string(message.signer);
    }
    if (message.domain !== "") {
      writer.uint32(18).string(message.domain);
    }
    if (message.privKey.length !== 0) {
      writer.uint32(26).bytes(message.privKey);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgRevokeDkimPubKey {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRevokeDkimPubKey();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.signer = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.domain = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.privKey = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgRevokeDkimPubKey {
    return {
      signer: isSet(object.signer) ? globalThis.String(object.signer) : "",
      domain: isSet(object.domain) ? globalThis.String(object.domain) : "",
      privKey: isSet(object.privKey)
        ? bytesFromBase64(object.privKey)
        : isSet(object.priv_key)
        ? bytesFromBase64(object.priv_key)
        : new Uint8Array(0),
    };
  },

  toJSON(message: MsgRevokeDkimPubKey): unknown {
    const obj: any = {};
    if (message.signer !== "") {
      obj.signer = message.signer;
    }
    if (message.domain !== "") {
      obj.domain = message.domain;
    }
    if (message.privKey.length !== 0) {
      obj.privKey = base64FromBytes(message.privKey);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgRevokeDkimPubKey>, I>>(base?: I): MsgRevokeDkimPubKey {
    return MsgRevokeDkimPubKey.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgRevokeDkimPubKey>, I>>(object: I): MsgRevokeDkimPubKey {
    const message = createBaseMsgRevokeDkimPubKey();
    message.signer = object.signer ?? "";
    message.domain = object.domain ?? "";
    message.privKey = object.privKey ?? new Uint8Array(0);
    return message;
  },
};

function createBaseMsgRevokeDkimPubKeyResponse(): MsgRevokeDkimPubKeyResponse {
  return {};
}

export const MsgRevokeDkimPubKeyResponse: MessageFns<MsgRevokeDkimPubKeyResponse> = {
  encode(_: MsgRevokeDkimPubKeyResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgRevokeDkimPubKeyResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRevokeDkimPubKeyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgRevokeDkimPubKeyResponse {
    return {};
  },

  toJSON(_: MsgRevokeDkimPubKeyResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgRevokeDkimPubKeyResponse>, I>>(base?: I): MsgRevokeDkimPubKeyResponse {
    return MsgRevokeDkimPubKeyResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgRevokeDkimPubKeyResponse>, I>>(_: I): MsgRevokeDkimPubKeyResponse {
    const message = createBaseMsgRevokeDkimPubKeyResponse();
    return message;
  },
};

/** Msg defines the Msg service. */
export interface Msg {
  /** AddDkimPubKey defines a message to add DKIM public keys. */
  AddDkimPubKeys(request: DeepPartial<MsgAddDkimPubKeys>, metadata?: grpc.Metadata): Promise<MsgAddDkimPubKeysResponse>;
  /**
   * RemoveDkimPubKey defines a message to remove a DKIM public key via
   * governance
   */
  RemoveDkimPubKey(
    request: DeepPartial<MsgRemoveDkimPubKey>,
    metadata?: grpc.Metadata,
  ): Promise<MsgRemoveDkimPubKeyResponse>;
  /**
   * RevokeDkimPubKey defines a message to remove a DKIM public key without
   * governance, by submitting the compromised privkey
   */
  RevokeDkimPubKey(
    request: DeepPartial<MsgRevokeDkimPubKey>,
    metadata?: grpc.Metadata,
  ): Promise<MsgRevokeDkimPubKeyResponse>;
  /**
   * UpdateParams defines a governance operation for updating the parameters.
   *
   * Since: cosmos-sdk 0.47
   */
  UpdateParams(request: DeepPartial<MsgUpdateParams>, metadata?: grpc.Metadata): Promise<MsgUpdateParamsResponse>;
}

export class MsgClientImpl implements Msg {
  private readonly rpc: Rpc;

  constructor(rpc: Rpc) {
    this.rpc = rpc;
    this.AddDkimPubKeys = this.AddDkimPubKeys.bind(this);
    this.RemoveDkimPubKey = this.RemoveDkimPubKey.bind(this);
    this.RevokeDkimPubKey = this.RevokeDkimPubKey.bind(this);
    this.UpdateParams = this.UpdateParams.bind(this);
  }

  AddDkimPubKeys(
    request: DeepPartial<MsgAddDkimPubKeys>,
    metadata?: grpc.Metadata,
  ): Promise<MsgAddDkimPubKeysResponse> {
    return this.rpc.unary(MsgAddDkimPubKeysDesc, MsgAddDkimPubKeys.fromPartial(request), metadata);
  }

  RemoveDkimPubKey(
    request: DeepPartial<MsgRemoveDkimPubKey>,
    metadata?: grpc.Metadata,
  ): Promise<MsgRemoveDkimPubKeyResponse> {
    return this.rpc.unary(MsgRemoveDkimPubKeyDesc, MsgRemoveDkimPubKey.fromPartial(request), metadata);
  }

  RevokeDkimPubKey(
    request: DeepPartial<MsgRevokeDkimPubKey>,
    metadata?: grpc.Metadata,
  ): Promise<MsgRevokeDkimPubKeyResponse> {
    return this.rpc.unary(MsgRevokeDkimPubKeyDesc, MsgRevokeDkimPubKey.fromPartial(request), metadata);
  }

  UpdateParams(request: DeepPartial<MsgUpdateParams>, metadata?: grpc.Metadata): Promise<MsgUpdateParamsResponse> {
    return this.rpc.unary(MsgUpdateParamsDesc, MsgUpdateParams.fromPartial(request), metadata);
  }
}

export const MsgDesc = { serviceName: "xion.dkim.v1.Msg" };

export const MsgAddDkimPubKeysDesc: UnaryMethodDefinitionish = {
  methodName: "AddDkimPubKeys",
  service: MsgDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return MsgAddDkimPubKeys.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = MsgAddDkimPubKeysResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const MsgRemoveDkimPubKeyDesc: UnaryMethodDefinitionish = {
  methodName: "RemoveDkimPubKey",
  service: MsgDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return MsgRemoveDkimPubKey.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = MsgRemoveDkimPubKeyResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const MsgRevokeDkimPubKeyDesc: UnaryMethodDefinitionish = {
  methodName: "RevokeDkimPubKey",
  service: MsgDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return MsgRevokeDkimPubKey.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = MsgRevokeDkimPubKeyResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const MsgUpdateParamsDesc: UnaryMethodDefinitionish = {
  methodName: "UpdateParams",
  service: MsgDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return MsgUpdateParams.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = MsgUpdateParamsResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

interface UnaryMethodDefinitionishR extends grpc.UnaryMethodDefinition<any, any> {
  requestStream: any;
  responseStream: any;
}

type UnaryMethodDefinitionish = UnaryMethodDefinitionishR;

interface Rpc {
  unary<T extends UnaryMethodDefinitionish>(
    methodDesc: T,
    request: any,
    metadata: grpc.Metadata | undefined,
  ): Promise<any>;
}

export class GrpcWebImpl {
  private host: string;
  private options: {
    transport?: grpc.TransportFactory;

    debug?: boolean;
    metadata?: grpc.Metadata;
    upStreamRetryCodes?: number[];
  };

  constructor(
    host: string,
    options: {
      transport?: grpc.TransportFactory;

      debug?: boolean;
      metadata?: grpc.Metadata;
      upStreamRetryCodes?: number[];
    },
  ) {
    this.host = host;
    this.options = options;
  }

  unary<T extends UnaryMethodDefinitionish>(
    methodDesc: T,
    _request: any,
    metadata: grpc.Metadata | undefined,
  ): Promise<any> {
    const request = { ..._request, ...methodDesc.requestType };
    const maybeCombinedMetadata = metadata && this.options.metadata
      ? new BrowserHeaders({ ...this.options?.metadata.headersMap, ...metadata?.headersMap })
      : metadata ?? this.options.metadata;
    return new Promise((resolve, reject) => {
      grpc.unary(methodDesc, {
        request,
        host: this.host,
        metadata: maybeCombinedMetadata ?? {},
        ...(this.options.transport !== undefined ? { transport: this.options.transport } : {}),
        debug: this.options.debug ?? false,
        onEnd: function (response) {
          if (response.status === grpc.Code.OK) {
            resolve(response.message!.toObject());
          } else {
            const err = new GrpcWebError(response.statusMessage, response.status, response.trailers);
            reject(err);
          }
        },
      });
    });
  }
}

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export class GrpcWebError extends globalThis.Error {
  constructor(message: string, public code: grpc.Code, public metadata: grpc.Metadata) {
    super(message);
  }
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
