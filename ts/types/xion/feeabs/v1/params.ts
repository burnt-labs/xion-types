// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.181.2
//   protoc               unknown
// source: xion/feeabs/v1/params.proto

/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal";

export const protobufPackage = "xion.feeabs.v1";

/** Params holds parameters for the feeabs module. */
export interface Params {
  /** Native fee token denomination */
  nativeFeeDenom: string;
  /** Osmosis pool timeout */
  poolTimeout: Long;
  /** IBC timeout */
  ibcTimeout: Long;
}

function createBaseParams(): Params {
  return { nativeFeeDenom: "", poolTimeout: Long.ZERO, ibcTimeout: Long.UZERO };
}

export const Params = {
  encode(message: Params, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.nativeFeeDenom !== "") {
      writer.uint32(10).string(message.nativeFeeDenom);
    }
    if (!message.poolTimeout.equals(Long.ZERO)) {
      writer.uint32(16).int64(message.poolTimeout);
    }
    if (!message.ibcTimeout.equals(Long.UZERO)) {
      writer.uint32(24).uint64(message.ibcTimeout);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Params {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseParams();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.nativeFeeDenom = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.poolTimeout = reader.int64() as Long;
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.ibcTimeout = reader.uint64() as Long;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Params {
    return {
      nativeFeeDenom: isSet(object.nativeFeeDenom) ? globalThis.String(object.nativeFeeDenom) : "",
      poolTimeout: isSet(object.poolTimeout) ? Long.fromValue(object.poolTimeout) : Long.ZERO,
      ibcTimeout: isSet(object.ibcTimeout) ? Long.fromValue(object.ibcTimeout) : Long.UZERO,
    };
  },

  toJSON(message: Params): unknown {
    const obj: any = {};
    if (message.nativeFeeDenom !== "") {
      obj.nativeFeeDenom = message.nativeFeeDenom;
    }
    if (!message.poolTimeout.equals(Long.ZERO)) {
      obj.poolTimeout = (message.poolTimeout || Long.ZERO).toString();
    }
    if (!message.ibcTimeout.equals(Long.UZERO)) {
      obj.ibcTimeout = (message.ibcTimeout || Long.UZERO).toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Params>, I>>(base?: I): Params {
    return Params.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Params>, I>>(object: I): Params {
    const message = createBaseParams();
    message.nativeFeeDenom = object.nativeFeeDenom ?? "";
    message.poolTimeout = (object.poolTimeout !== undefined && object.poolTimeout !== null)
      ? Long.fromValue(object.poolTimeout)
      : Long.ZERO;
    message.ibcTimeout = (object.ibcTimeout !== undefined && object.ibcTimeout !== null)
      ? Long.fromValue(object.ibcTimeout)
      : Long.UZERO;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
