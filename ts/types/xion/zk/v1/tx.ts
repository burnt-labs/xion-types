// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               unknown
// source: xion/zk/v1/tx.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { grpc } from "@improbable-eng/grpc-web";
import { BrowserHeaders } from "browser-headers";
import { Params } from "./params";

export const protobufPackage = "xion.zk.v1";

/** MsgAddVKey is the message for adding a verification key */
export interface MsgAddVKey {
  /** authority is the address that controls the module (governance) */
  authority: string;
  /** name is the unique identifier for this verification key */
  name: string;
  /** description provides context about this verification key */
  description: string;
  /** vkey_json is the JSON representation of the verification key */
  vkeyBytes: Uint8Array;
}

/** MsgAddVKeyResponse is the response for MsgAddVKey */
export interface MsgAddVKeyResponse {
  /** id is the unique numeric identifier assigned to the vkey */
  id: bigint;
}

/** MsgUpdateVKey is the message for updating a verification key */
export interface MsgUpdateVKey {
  /** authority is the address that controls the module (governance) */
  authority: string;
  /** name is the identifier of the verification key to update */
  name: string;
  /** description provides updated context */
  description: string;
  /** vkey_json is the new JSON representation of the verification key */
  vkeyBytes: Uint8Array;
}

/** MsgUpdateVKeyResponse is the response for MsgUpdateVKey */
export interface MsgUpdateVKeyResponse {
}

/** MsgRemoveVKey is the message for removing a verification key */
export interface MsgRemoveVKey {
  /** authority is the address that controls the module (governance) */
  authority: string;
  /** name is the identifier of the verification key to remove */
  name: string;
}

/** MsgRemoveVKeyResponse is the response for MsgRemoveVKey */
export interface MsgRemoveVKeyResponse {
}

/** MsgUpdateParams is the Msg/UpdateParams request type. */
export interface MsgUpdateParams {
  /** authority is the address of the governance account. */
  authority: string;
  /**
   * params defines the parameters to update.
   *
   * NOTE: All parameters must be supplied.
   */
  params?: Params | undefined;
}

/**
 * MsgUpdateParamsResponse defines the response structure for executing a
 * MsgUpdateParams message.
 */
export interface MsgUpdateParamsResponse {
}

function createBaseMsgAddVKey(): MsgAddVKey {
  return { authority: "", name: "", description: "", vkeyBytes: new Uint8Array(0) };
}

export const MsgAddVKey: MessageFns<MsgAddVKey> = {
  encode(message: MsgAddVKey, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.authority !== "") {
      writer.uint32(10).string(message.authority);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.description !== "") {
      writer.uint32(26).string(message.description);
    }
    if (message.vkeyBytes.length !== 0) {
      writer.uint32(34).bytes(message.vkeyBytes);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgAddVKey {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgAddVKey();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.authority = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.vkeyBytes = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgAddVKey {
    return {
      authority: isSet(object.authority) ? globalThis.String(object.authority) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      vkeyBytes: isSet(object.vkeyBytes)
        ? bytesFromBase64(object.vkeyBytes)
        : isSet(object.vkey_bytes)
        ? bytesFromBase64(object.vkey_bytes)
        : new Uint8Array(0),
    };
  },

  toJSON(message: MsgAddVKey): unknown {
    const obj: any = {};
    if (message.authority !== "") {
      obj.authority = message.authority;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.vkeyBytes.length !== 0) {
      obj.vkeyBytes = base64FromBytes(message.vkeyBytes);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgAddVKey>, I>>(base?: I): MsgAddVKey {
    return MsgAddVKey.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgAddVKey>, I>>(object: I): MsgAddVKey {
    const message = createBaseMsgAddVKey();
    message.authority = object.authority ?? "";
    message.name = object.name ?? "";
    message.description = object.description ?? "";
    message.vkeyBytes = object.vkeyBytes ?? new Uint8Array(0);
    return message;
  },
};

function createBaseMsgAddVKeyResponse(): MsgAddVKeyResponse {
  return { id: 0n };
}

export const MsgAddVKeyResponse: MessageFns<MsgAddVKeyResponse> = {
  encode(message: MsgAddVKeyResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0n) {
      if (BigInt.asUintN(64, message.id) !== message.id) {
        throw new globalThis.Error("value provided for field message.id of type uint64 too large");
      }
      writer.uint32(8).uint64(message.id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgAddVKeyResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgAddVKeyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgAddVKeyResponse {
    return { id: isSet(object.id) ? BigInt(object.id) : 0n };
  },

  toJSON(message: MsgAddVKeyResponse): unknown {
    const obj: any = {};
    if (message.id !== 0n) {
      obj.id = message.id.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgAddVKeyResponse>, I>>(base?: I): MsgAddVKeyResponse {
    return MsgAddVKeyResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgAddVKeyResponse>, I>>(object: I): MsgAddVKeyResponse {
    const message = createBaseMsgAddVKeyResponse();
    message.id = object.id ?? 0n;
    return message;
  },
};

function createBaseMsgUpdateVKey(): MsgUpdateVKey {
  return { authority: "", name: "", description: "", vkeyBytes: new Uint8Array(0) };
}

export const MsgUpdateVKey: MessageFns<MsgUpdateVKey> = {
  encode(message: MsgUpdateVKey, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.authority !== "") {
      writer.uint32(10).string(message.authority);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.description !== "") {
      writer.uint32(26).string(message.description);
    }
    if (message.vkeyBytes.length !== 0) {
      writer.uint32(34).bytes(message.vkeyBytes);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgUpdateVKey {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgUpdateVKey();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.authority = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.vkeyBytes = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgUpdateVKey {
    return {
      authority: isSet(object.authority) ? globalThis.String(object.authority) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      vkeyBytes: isSet(object.vkeyBytes)
        ? bytesFromBase64(object.vkeyBytes)
        : isSet(object.vkey_bytes)
        ? bytesFromBase64(object.vkey_bytes)
        : new Uint8Array(0),
    };
  },

  toJSON(message: MsgUpdateVKey): unknown {
    const obj: any = {};
    if (message.authority !== "") {
      obj.authority = message.authority;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.vkeyBytes.length !== 0) {
      obj.vkeyBytes = base64FromBytes(message.vkeyBytes);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgUpdateVKey>, I>>(base?: I): MsgUpdateVKey {
    return MsgUpdateVKey.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgUpdateVKey>, I>>(object: I): MsgUpdateVKey {
    const message = createBaseMsgUpdateVKey();
    message.authority = object.authority ?? "";
    message.name = object.name ?? "";
    message.description = object.description ?? "";
    message.vkeyBytes = object.vkeyBytes ?? new Uint8Array(0);
    return message;
  },
};

function createBaseMsgUpdateVKeyResponse(): MsgUpdateVKeyResponse {
  return {};
}

export const MsgUpdateVKeyResponse: MessageFns<MsgUpdateVKeyResponse> = {
  encode(_: MsgUpdateVKeyResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgUpdateVKeyResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgUpdateVKeyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgUpdateVKeyResponse {
    return {};
  },

  toJSON(_: MsgUpdateVKeyResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgUpdateVKeyResponse>, I>>(base?: I): MsgUpdateVKeyResponse {
    return MsgUpdateVKeyResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgUpdateVKeyResponse>, I>>(_: I): MsgUpdateVKeyResponse {
    const message = createBaseMsgUpdateVKeyResponse();
    return message;
  },
};

function createBaseMsgRemoveVKey(): MsgRemoveVKey {
  return { authority: "", name: "" };
}

export const MsgRemoveVKey: MessageFns<MsgRemoveVKey> = {
  encode(message: MsgRemoveVKey, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.authority !== "") {
      writer.uint32(10).string(message.authority);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgRemoveVKey {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRemoveVKey();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.authority = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgRemoveVKey {
    return {
      authority: isSet(object.authority) ? globalThis.String(object.authority) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
    };
  },

  toJSON(message: MsgRemoveVKey): unknown {
    const obj: any = {};
    if (message.authority !== "") {
      obj.authority = message.authority;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgRemoveVKey>, I>>(base?: I): MsgRemoveVKey {
    return MsgRemoveVKey.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgRemoveVKey>, I>>(object: I): MsgRemoveVKey {
    const message = createBaseMsgRemoveVKey();
    message.authority = object.authority ?? "";
    message.name = object.name ?? "";
    return message;
  },
};

function createBaseMsgRemoveVKeyResponse(): MsgRemoveVKeyResponse {
  return {};
}

export const MsgRemoveVKeyResponse: MessageFns<MsgRemoveVKeyResponse> = {
  encode(_: MsgRemoveVKeyResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgRemoveVKeyResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgRemoveVKeyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgRemoveVKeyResponse {
    return {};
  },

  toJSON(_: MsgRemoveVKeyResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgRemoveVKeyResponse>, I>>(base?: I): MsgRemoveVKeyResponse {
    return MsgRemoveVKeyResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgRemoveVKeyResponse>, I>>(_: I): MsgRemoveVKeyResponse {
    const message = createBaseMsgRemoveVKeyResponse();
    return message;
  },
};

function createBaseMsgUpdateParams(): MsgUpdateParams {
  return { authority: "", params: undefined };
}

export const MsgUpdateParams: MessageFns<MsgUpdateParams> = {
  encode(message: MsgUpdateParams, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.authority !== "") {
      writer.uint32(10).string(message.authority);
    }
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgUpdateParams {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgUpdateParams();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.authority = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.params = Params.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MsgUpdateParams {
    return {
      authority: isSet(object.authority) ? globalThis.String(object.authority) : "",
      params: isSet(object.params) ? Params.fromJSON(object.params) : undefined,
    };
  },

  toJSON(message: MsgUpdateParams): unknown {
    const obj: any = {};
    if (message.authority !== "") {
      obj.authority = message.authority;
    }
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgUpdateParams>, I>>(base?: I): MsgUpdateParams {
    return MsgUpdateParams.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgUpdateParams>, I>>(object: I): MsgUpdateParams {
    const message = createBaseMsgUpdateParams();
    message.authority = object.authority ?? "";
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    return message;
  },
};

function createBaseMsgUpdateParamsResponse(): MsgUpdateParamsResponse {
  return {};
}

export const MsgUpdateParamsResponse: MessageFns<MsgUpdateParamsResponse> = {
  encode(_: MsgUpdateParamsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MsgUpdateParamsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMsgUpdateParamsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): MsgUpdateParamsResponse {
    return {};
  },

  toJSON(_: MsgUpdateParamsResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<MsgUpdateParamsResponse>, I>>(base?: I): MsgUpdateParamsResponse {
    return MsgUpdateParamsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MsgUpdateParamsResponse>, I>>(_: I): MsgUpdateParamsResponse {
    const message = createBaseMsgUpdateParamsResponse();
    return message;
  },
};

/** Msg defines the Msg service. */
export interface Msg {
  /** AddVKey adds a new verification key */
  AddVKey(request: DeepPartial<MsgAddVKey>, metadata?: grpc.Metadata): Promise<MsgAddVKeyResponse>;
  /** UpdateVKey updates an existing verification key */
  UpdateVKey(request: DeepPartial<MsgUpdateVKey>, metadata?: grpc.Metadata): Promise<MsgUpdateVKeyResponse>;
  /** RemoveVKey removes a verification key */
  RemoveVKey(request: DeepPartial<MsgRemoveVKey>, metadata?: grpc.Metadata): Promise<MsgRemoveVKeyResponse>;
  /** UpdateParams updates zk module parameters via governance. */
  UpdateParams(request: DeepPartial<MsgUpdateParams>, metadata?: grpc.Metadata): Promise<MsgUpdateParamsResponse>;
}

export class MsgClientImpl implements Msg {
  private readonly rpc: Rpc;

  constructor(rpc: Rpc) {
    this.rpc = rpc;
    this.AddVKey = this.AddVKey.bind(this);
    this.UpdateVKey = this.UpdateVKey.bind(this);
    this.RemoveVKey = this.RemoveVKey.bind(this);
    this.UpdateParams = this.UpdateParams.bind(this);
  }

  AddVKey(request: DeepPartial<MsgAddVKey>, metadata?: grpc.Metadata): Promise<MsgAddVKeyResponse> {
    return this.rpc.unary(MsgAddVKeyDesc, MsgAddVKey.fromPartial(request), metadata);
  }

  UpdateVKey(request: DeepPartial<MsgUpdateVKey>, metadata?: grpc.Metadata): Promise<MsgUpdateVKeyResponse> {
    return this.rpc.unary(MsgUpdateVKeyDesc, MsgUpdateVKey.fromPartial(request), metadata);
  }

  RemoveVKey(request: DeepPartial<MsgRemoveVKey>, metadata?: grpc.Metadata): Promise<MsgRemoveVKeyResponse> {
    return this.rpc.unary(MsgRemoveVKeyDesc, MsgRemoveVKey.fromPartial(request), metadata);
  }

  UpdateParams(request: DeepPartial<MsgUpdateParams>, metadata?: grpc.Metadata): Promise<MsgUpdateParamsResponse> {
    return this.rpc.unary(MsgUpdateParamsDesc, MsgUpdateParams.fromPartial(request), metadata);
  }
}

export const MsgDesc = { serviceName: "xion.zk.v1.Msg" };

export const MsgAddVKeyDesc: UnaryMethodDefinitionish = {
  methodName: "AddVKey",
  service: MsgDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return MsgAddVKey.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = MsgAddVKeyResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const MsgUpdateVKeyDesc: UnaryMethodDefinitionish = {
  methodName: "UpdateVKey",
  service: MsgDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return MsgUpdateVKey.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = MsgUpdateVKeyResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const MsgRemoveVKeyDesc: UnaryMethodDefinitionish = {
  methodName: "RemoveVKey",
  service: MsgDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return MsgRemoveVKey.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = MsgRemoveVKeyResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const MsgUpdateParamsDesc: UnaryMethodDefinitionish = {
  methodName: "UpdateParams",
  service: MsgDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return MsgUpdateParams.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = MsgUpdateParamsResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export interface UnaryMethodDefinitionishR extends grpc.UnaryMethodDefinition<any, any> {
  requestStream: any;
  responseStream: any;
}

type UnaryMethodDefinitionish = UnaryMethodDefinitionishR;

interface Rpc {
  unary<T extends UnaryMethodDefinitionish>(
    methodDesc: T,
    request: any,
    metadata: grpc.Metadata | undefined,
  ): Promise<any>;
}

export class GrpcWebImpl {
  private host: string;
  private options: {
    transport?: grpc.TransportFactory;

    debug?: boolean;
    metadata?: grpc.Metadata;
    upStreamRetryCodes?: number[];
  };

  constructor(
    host: string,
    options: {
      transport?: grpc.TransportFactory;

      debug?: boolean;
      metadata?: grpc.Metadata;
      upStreamRetryCodes?: number[];
    },
  ) {
    this.host = host;
    this.options = options;
  }

  unary<T extends UnaryMethodDefinitionish>(
    methodDesc: T,
    _request: any,
    metadata: grpc.Metadata | undefined,
  ): Promise<any> {
    const request = { ..._request, ...methodDesc.requestType };
    const maybeCombinedMetadata = metadata && this.options.metadata
      ? new BrowserHeaders({ ...this.options?.metadata.headersMap, ...metadata?.headersMap })
      : metadata ?? this.options.metadata;
    return new Promise((resolve, reject) => {
      grpc.unary(methodDesc, {
        request,
        host: this.host,
        metadata: maybeCombinedMetadata ?? {},
        ...(this.options.transport !== undefined ? { transport: this.options.transport } : {}),
        debug: this.options.debug ?? false,
        onEnd: function (response) {
          if (response.status === grpc.Code.OK) {
            resolve(response.message!.toObject());
          } else {
            const err = new GrpcWebError(response.statusMessage, response.status, response.trailers);
            reject(err);
          }
        },
      });
    });
  }
}

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export class GrpcWebError extends globalThis.Error {
  constructor(message: string, public code: grpc.Code, public metadata: grpc.Metadata) {
    super(message);
  }
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
