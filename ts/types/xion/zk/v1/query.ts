// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: xion/zk/v1/query.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { grpc } from "@improbable-eng/grpc-web";
import { BrowserHeaders } from "browser-headers";
import { PageRequest, PageResponse } from "../../../cosmos/base/query/v1beta1/pagination";
import { Params } from "./params";

export const protobufPackage = "xion.zk.v1";

/** SnarkJsProof represents a ZK-SNARK proof in SnarkJS format. */
export interface SnarkJsProof {
  /** pi_a defines the first component of the proof. */
  piA: Uint8Array[];
  /** pi_b defines the second component of the proof. */
  piB: Uint8Array[];
  /** pi_c defines the third component of the proof. */
  piC: Uint8Array[];
}

/** QueryVerifyRequest is the request type for the Query/ProofVerify RPC method. */
export interface QueryVerifyRequest {
  /** proof is the serialized ZK proof to verify. */
  proof: Uint8Array;
  /** public_inputs is the list of public inputs for the proof. */
  publicInputs: string[];
  /** vkey_name is the name of the verification key to use. */
  vkeyName: string;
  /** vkey_id is the ID of the verification key to use. */
  vkeyId: bigint;
}

/** ProofVerifyResponse defines the response structure for proof verification. */
export interface ProofVerifyResponse {
  /** verified indicates whether the proof verification was successful. */
  verified: boolean;
}

/** VKey represents a verification key for ZK proof verification. */
export interface VKey {
  /** key_bytes is the raw bytes of the verification key. */
  keyBytes: Uint8Array;
  /** name is the unique human-readable identifier for this key. */
  name: string;
  /** description provides additional context about the verification key. */
  description: string;
  /** circuit_hash is the hash of the circuit this key is associated with. */
  circuitHash: string;
  /** authority is the uploader of the verification key. */
  authority: string;
}

/** QueryVKeyRequest is the request type for the Query/VKey RPC method */
export interface QueryVKeyRequest {
  /** id is the unique identifier of the verification key */
  id: bigint;
}

/** QueryVKeyResponse is the response type for the Query/VKey RPC method */
export interface QueryVKeyResponse {
  /** vkey is the verification key */
  vkey?: VKey | undefined;
}

/**
 * QueryVKeyByNameRequest is the request type for the Query/VKeyByName RPC
 * method
 */
export interface QueryVKeyByNameRequest {
  /** name is the unique name of the verification key */
  name: string;
}

/**
 * QueryVKeyByNameResponse is the response type for the Query/VKeyByName RPC
 * method
 */
export interface QueryVKeyByNameResponse {
  /** vkey is the verification key */
  vkey?:
    | VKey
    | undefined;
  /** id is the numeric identifier of the verification key */
  id: bigint;
}

/** QueryVKeysRequest is the request type for the Query/VKeys RPC method */
export interface QueryVKeysRequest {
  /** pagination defines an optional pagination for the request */
  pagination?: PageRequest | undefined;
}

/** QueryVKeysResponse is the response type for the Query/VKeys RPC method */
export interface QueryVKeysResponse {
  /** vkeys is the list of all verification keys with their IDs */
  vkeys: VKeyWithID[];
  /** pagination defines the pagination in the response */
  pagination?: PageResponse | undefined;
}

/** VKeyWithID combines a verification key with its ID */
export interface VKeyWithID {
  /** id is the unique identifier */
  id: bigint;
  /** vkey is the verification key */
  vkey?: VKey | undefined;
}

/** QueryHasVKeyRequest is the request type for the Query/HasVKey RPC method */
export interface QueryHasVKeyRequest {
  /** name is the name of the verification key to check */
  name: string;
}

/** QueryHasVKeyResponse is the response type for the Query/HasVKey RPC method */
export interface QueryHasVKeyResponse {
  /** exists indicates whether the verification key exists */
  exists: boolean;
  /** id is the numeric identifier if the key exists (0 if not found) */
  id: bigint;
}

/**
 * QueryNextVKeyIDRequest is the request type for the Query/NextVKeyID RPC
 * method
 */
export interface QueryNextVKeyIDRequest {
}

/**
 * QueryNextVKeyIDResponse is the response type for the Query/NextVKeyID RPC
 * method
 */
export interface QueryNextVKeyIDResponse {
  /** next_id is the next available verification key ID. */
  nextId: bigint;
}

/** QueryParamsRequest is the request type for the Query/Params RPC method */
export interface QueryParamsRequest {
}

/** QueryParamsResponse is the response type for the Query/Params RPC method */
export interface QueryParamsResponse {
  /** params is the current zk module parameters */
  params?: Params | undefined;
}

function createBaseSnarkJsProof(): SnarkJsProof {
  return { piA: [], piB: [], piC: [] };
}

export const SnarkJsProof: MessageFns<SnarkJsProof> = {
  encode(message: SnarkJsProof, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.piA) {
      writer.uint32(10).bytes(v!);
    }
    for (const v of message.piB) {
      writer.uint32(18).bytes(v!);
    }
    for (const v of message.piC) {
      writer.uint32(26).bytes(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SnarkJsProof {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSnarkJsProof();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.piA.push(reader.bytes());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.piB.push(reader.bytes());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.piC.push(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SnarkJsProof {
    return {
      piA: globalThis.Array.isArray(object?.piA)
        ? object.piA.map((e: any) => bytesFromBase64(e))
        : globalThis.Array.isArray(object?.pi_a)
        ? object.pi_a.map((e: any) => bytesFromBase64(e))
        : [],
      piB: globalThis.Array.isArray(object?.piB)
        ? object.piB.map((e: any) => bytesFromBase64(e))
        : globalThis.Array.isArray(object?.pi_b)
        ? object.pi_b.map((e: any) => bytesFromBase64(e))
        : [],
      piC: globalThis.Array.isArray(object?.piC)
        ? object.piC.map((e: any) => bytesFromBase64(e))
        : globalThis.Array.isArray(object?.pi_c)
        ? object.pi_c.map((e: any) => bytesFromBase64(e))
        : [],
    };
  },

  toJSON(message: SnarkJsProof): unknown {
    const obj: any = {};
    if (message.piA?.length) {
      obj.piA = message.piA.map((e) => base64FromBytes(e));
    }
    if (message.piB?.length) {
      obj.piB = message.piB.map((e) => base64FromBytes(e));
    }
    if (message.piC?.length) {
      obj.piC = message.piC.map((e) => base64FromBytes(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SnarkJsProof>, I>>(base?: I): SnarkJsProof {
    return SnarkJsProof.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SnarkJsProof>, I>>(object: I): SnarkJsProof {
    const message = createBaseSnarkJsProof();
    message.piA = object.piA?.map((e) => e) || [];
    message.piB = object.piB?.map((e) => e) || [];
    message.piC = object.piC?.map((e) => e) || [];
    return message;
  },
};

function createBaseQueryVerifyRequest(): QueryVerifyRequest {
  return { proof: new Uint8Array(0), publicInputs: [], vkeyName: "", vkeyId: 0n };
}

export const QueryVerifyRequest: MessageFns<QueryVerifyRequest> = {
  encode(message: QueryVerifyRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.proof.length !== 0) {
      writer.uint32(10).bytes(message.proof);
    }
    for (const v of message.publicInputs) {
      writer.uint32(18).string(v!);
    }
    if (message.vkeyName !== "") {
      writer.uint32(26).string(message.vkeyName);
    }
    if (message.vkeyId !== 0n) {
      if (BigInt.asUintN(64, message.vkeyId) !== message.vkeyId) {
        throw new globalThis.Error("value provided for field message.vkeyId of type uint64 too large");
      }
      writer.uint32(32).uint64(message.vkeyId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryVerifyRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryVerifyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.proof = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.publicInputs.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.vkeyName = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.vkeyId = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryVerifyRequest {
    return {
      proof: isSet(object.proof) ? bytesFromBase64(object.proof) : new Uint8Array(0),
      publicInputs: globalThis.Array.isArray(object?.publicInputs)
        ? object.publicInputs.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.public_inputs)
        ? object.public_inputs.map((e: any) => globalThis.String(e))
        : [],
      vkeyName: isSet(object.vkeyName)
        ? globalThis.String(object.vkeyName)
        : isSet(object.vkey_name)
        ? globalThis.String(object.vkey_name)
        : "",
      vkeyId: isSet(object.vkeyId) ? BigInt(object.vkeyId) : isSet(object.vkey_id) ? BigInt(object.vkey_id) : 0n,
    };
  },

  toJSON(message: QueryVerifyRequest): unknown {
    const obj: any = {};
    if (message.proof.length !== 0) {
      obj.proof = base64FromBytes(message.proof);
    }
    if (message.publicInputs?.length) {
      obj.publicInputs = message.publicInputs;
    }
    if (message.vkeyName !== "") {
      obj.vkeyName = message.vkeyName;
    }
    if (message.vkeyId !== 0n) {
      obj.vkeyId = message.vkeyId.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryVerifyRequest>, I>>(base?: I): QueryVerifyRequest {
    return QueryVerifyRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryVerifyRequest>, I>>(object: I): QueryVerifyRequest {
    const message = createBaseQueryVerifyRequest();
    message.proof = object.proof ?? new Uint8Array(0);
    message.publicInputs = object.publicInputs?.map((e) => e) || [];
    message.vkeyName = object.vkeyName ?? "";
    message.vkeyId = object.vkeyId ?? 0n;
    return message;
  },
};

function createBaseProofVerifyResponse(): ProofVerifyResponse {
  return { verified: false };
}

export const ProofVerifyResponse: MessageFns<ProofVerifyResponse> = {
  encode(message: ProofVerifyResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.verified !== false) {
      writer.uint32(8).bool(message.verified);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProofVerifyResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProofVerifyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.verified = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProofVerifyResponse {
    return { verified: isSet(object.verified) ? globalThis.Boolean(object.verified) : false };
  },

  toJSON(message: ProofVerifyResponse): unknown {
    const obj: any = {};
    if (message.verified !== false) {
      obj.verified = message.verified;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProofVerifyResponse>, I>>(base?: I): ProofVerifyResponse {
    return ProofVerifyResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProofVerifyResponse>, I>>(object: I): ProofVerifyResponse {
    const message = createBaseProofVerifyResponse();
    message.verified = object.verified ?? false;
    return message;
  },
};

function createBaseVKey(): VKey {
  return { keyBytes: new Uint8Array(0), name: "", description: "", circuitHash: "", authority: "" };
}

export const VKey: MessageFns<VKey> = {
  encode(message: VKey, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.keyBytes.length !== 0) {
      writer.uint32(10).bytes(message.keyBytes);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.description !== "") {
      writer.uint32(26).string(message.description);
    }
    if (message.circuitHash !== "") {
      writer.uint32(34).string(message.circuitHash);
    }
    if (message.authority !== "") {
      writer.uint32(42).string(message.authority);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VKey {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVKey();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.keyBytes = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.circuitHash = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.authority = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VKey {
    return {
      keyBytes: isSet(object.keyBytes)
        ? bytesFromBase64(object.keyBytes)
        : isSet(object.key_bytes)
        ? bytesFromBase64(object.key_bytes)
        : new Uint8Array(0),
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      circuitHash: isSet(object.circuitHash)
        ? globalThis.String(object.circuitHash)
        : isSet(object.circuit_hash)
        ? globalThis.String(object.circuit_hash)
        : "",
      authority: isSet(object.authority) ? globalThis.String(object.authority) : "",
    };
  },

  toJSON(message: VKey): unknown {
    const obj: any = {};
    if (message.keyBytes.length !== 0) {
      obj.keyBytes = base64FromBytes(message.keyBytes);
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.circuitHash !== "") {
      obj.circuitHash = message.circuitHash;
    }
    if (message.authority !== "") {
      obj.authority = message.authority;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VKey>, I>>(base?: I): VKey {
    return VKey.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VKey>, I>>(object: I): VKey {
    const message = createBaseVKey();
    message.keyBytes = object.keyBytes ?? new Uint8Array(0);
    message.name = object.name ?? "";
    message.description = object.description ?? "";
    message.circuitHash = object.circuitHash ?? "";
    message.authority = object.authority ?? "";
    return message;
  },
};

function createBaseQueryVKeyRequest(): QueryVKeyRequest {
  return { id: 0n };
}

export const QueryVKeyRequest: MessageFns<QueryVKeyRequest> = {
  encode(message: QueryVKeyRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0n) {
      if (BigInt.asUintN(64, message.id) !== message.id) {
        throw new globalThis.Error("value provided for field message.id of type uint64 too large");
      }
      writer.uint32(8).uint64(message.id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryVKeyRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryVKeyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryVKeyRequest {
    return { id: isSet(object.id) ? BigInt(object.id) : 0n };
  },

  toJSON(message: QueryVKeyRequest): unknown {
    const obj: any = {};
    if (message.id !== 0n) {
      obj.id = message.id.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryVKeyRequest>, I>>(base?: I): QueryVKeyRequest {
    return QueryVKeyRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryVKeyRequest>, I>>(object: I): QueryVKeyRequest {
    const message = createBaseQueryVKeyRequest();
    message.id = object.id ?? 0n;
    return message;
  },
};

function createBaseQueryVKeyResponse(): QueryVKeyResponse {
  return { vkey: undefined };
}

export const QueryVKeyResponse: MessageFns<QueryVKeyResponse> = {
  encode(message: QueryVKeyResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.vkey !== undefined) {
      VKey.encode(message.vkey, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryVKeyResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryVKeyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.vkey = VKey.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryVKeyResponse {
    return { vkey: isSet(object.vkey) ? VKey.fromJSON(object.vkey) : undefined };
  },

  toJSON(message: QueryVKeyResponse): unknown {
    const obj: any = {};
    if (message.vkey !== undefined) {
      obj.vkey = VKey.toJSON(message.vkey);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryVKeyResponse>, I>>(base?: I): QueryVKeyResponse {
    return QueryVKeyResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryVKeyResponse>, I>>(object: I): QueryVKeyResponse {
    const message = createBaseQueryVKeyResponse();
    message.vkey = (object.vkey !== undefined && object.vkey !== null) ? VKey.fromPartial(object.vkey) : undefined;
    return message;
  },
};

function createBaseQueryVKeyByNameRequest(): QueryVKeyByNameRequest {
  return { name: "" };
}

export const QueryVKeyByNameRequest: MessageFns<QueryVKeyByNameRequest> = {
  encode(message: QueryVKeyByNameRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryVKeyByNameRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryVKeyByNameRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryVKeyByNameRequest {
    return { name: isSet(object.name) ? globalThis.String(object.name) : "" };
  },

  toJSON(message: QueryVKeyByNameRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryVKeyByNameRequest>, I>>(base?: I): QueryVKeyByNameRequest {
    return QueryVKeyByNameRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryVKeyByNameRequest>, I>>(object: I): QueryVKeyByNameRequest {
    const message = createBaseQueryVKeyByNameRequest();
    message.name = object.name ?? "";
    return message;
  },
};

function createBaseQueryVKeyByNameResponse(): QueryVKeyByNameResponse {
  return { vkey: undefined, id: 0n };
}

export const QueryVKeyByNameResponse: MessageFns<QueryVKeyByNameResponse> = {
  encode(message: QueryVKeyByNameResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.vkey !== undefined) {
      VKey.encode(message.vkey, writer.uint32(10).fork()).join();
    }
    if (message.id !== 0n) {
      if (BigInt.asUintN(64, message.id) !== message.id) {
        throw new globalThis.Error("value provided for field message.id of type uint64 too large");
      }
      writer.uint32(16).uint64(message.id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryVKeyByNameResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryVKeyByNameResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.vkey = VKey.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.id = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryVKeyByNameResponse {
    return {
      vkey: isSet(object.vkey) ? VKey.fromJSON(object.vkey) : undefined,
      id: isSet(object.id) ? BigInt(object.id) : 0n,
    };
  },

  toJSON(message: QueryVKeyByNameResponse): unknown {
    const obj: any = {};
    if (message.vkey !== undefined) {
      obj.vkey = VKey.toJSON(message.vkey);
    }
    if (message.id !== 0n) {
      obj.id = message.id.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryVKeyByNameResponse>, I>>(base?: I): QueryVKeyByNameResponse {
    return QueryVKeyByNameResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryVKeyByNameResponse>, I>>(object: I): QueryVKeyByNameResponse {
    const message = createBaseQueryVKeyByNameResponse();
    message.vkey = (object.vkey !== undefined && object.vkey !== null) ? VKey.fromPartial(object.vkey) : undefined;
    message.id = object.id ?? 0n;
    return message;
  },
};

function createBaseQueryVKeysRequest(): QueryVKeysRequest {
  return { pagination: undefined };
}

export const QueryVKeysRequest: MessageFns<QueryVKeysRequest> = {
  encode(message: QueryVKeysRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pagination !== undefined) {
      PageRequest.encode(message.pagination, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryVKeysRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryVKeysRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.pagination = PageRequest.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryVKeysRequest {
    return { pagination: isSet(object.pagination) ? PageRequest.fromJSON(object.pagination) : undefined };
  },

  toJSON(message: QueryVKeysRequest): unknown {
    const obj: any = {};
    if (message.pagination !== undefined) {
      obj.pagination = PageRequest.toJSON(message.pagination);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryVKeysRequest>, I>>(base?: I): QueryVKeysRequest {
    return QueryVKeysRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryVKeysRequest>, I>>(object: I): QueryVKeysRequest {
    const message = createBaseQueryVKeysRequest();
    message.pagination = (object.pagination !== undefined && object.pagination !== null)
      ? PageRequest.fromPartial(object.pagination)
      : undefined;
    return message;
  },
};

function createBaseQueryVKeysResponse(): QueryVKeysResponse {
  return { vkeys: [], pagination: undefined };
}

export const QueryVKeysResponse: MessageFns<QueryVKeysResponse> = {
  encode(message: QueryVKeysResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.vkeys) {
      VKeyWithID.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.pagination !== undefined) {
      PageResponse.encode(message.pagination, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryVKeysResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryVKeysResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.vkeys.push(VKeyWithID.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.pagination = PageResponse.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryVKeysResponse {
    return {
      vkeys: globalThis.Array.isArray(object?.vkeys) ? object.vkeys.map((e: any) => VKeyWithID.fromJSON(e)) : [],
      pagination: isSet(object.pagination) ? PageResponse.fromJSON(object.pagination) : undefined,
    };
  },

  toJSON(message: QueryVKeysResponse): unknown {
    const obj: any = {};
    if (message.vkeys?.length) {
      obj.vkeys = message.vkeys.map((e) => VKeyWithID.toJSON(e));
    }
    if (message.pagination !== undefined) {
      obj.pagination = PageResponse.toJSON(message.pagination);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryVKeysResponse>, I>>(base?: I): QueryVKeysResponse {
    return QueryVKeysResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryVKeysResponse>, I>>(object: I): QueryVKeysResponse {
    const message = createBaseQueryVKeysResponse();
    message.vkeys = object.vkeys?.map((e) => VKeyWithID.fromPartial(e)) || [];
    message.pagination = (object.pagination !== undefined && object.pagination !== null)
      ? PageResponse.fromPartial(object.pagination)
      : undefined;
    return message;
  },
};

function createBaseVKeyWithID(): VKeyWithID {
  return { id: 0n, vkey: undefined };
}

export const VKeyWithID: MessageFns<VKeyWithID> = {
  encode(message: VKeyWithID, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0n) {
      if (BigInt.asUintN(64, message.id) !== message.id) {
        throw new globalThis.Error("value provided for field message.id of type uint64 too large");
      }
      writer.uint32(8).uint64(message.id);
    }
    if (message.vkey !== undefined) {
      VKey.encode(message.vkey, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VKeyWithID {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVKeyWithID();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.id = reader.uint64() as bigint;
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.vkey = VKey.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VKeyWithID {
    return {
      id: isSet(object.id) ? BigInt(object.id) : 0n,
      vkey: isSet(object.vkey) ? VKey.fromJSON(object.vkey) : undefined,
    };
  },

  toJSON(message: VKeyWithID): unknown {
    const obj: any = {};
    if (message.id !== 0n) {
      obj.id = message.id.toString();
    }
    if (message.vkey !== undefined) {
      obj.vkey = VKey.toJSON(message.vkey);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VKeyWithID>, I>>(base?: I): VKeyWithID {
    return VKeyWithID.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VKeyWithID>, I>>(object: I): VKeyWithID {
    const message = createBaseVKeyWithID();
    message.id = object.id ?? 0n;
    message.vkey = (object.vkey !== undefined && object.vkey !== null) ? VKey.fromPartial(object.vkey) : undefined;
    return message;
  },
};

function createBaseQueryHasVKeyRequest(): QueryHasVKeyRequest {
  return { name: "" };
}

export const QueryHasVKeyRequest: MessageFns<QueryHasVKeyRequest> = {
  encode(message: QueryHasVKeyRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryHasVKeyRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryHasVKeyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryHasVKeyRequest {
    return { name: isSet(object.name) ? globalThis.String(object.name) : "" };
  },

  toJSON(message: QueryHasVKeyRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryHasVKeyRequest>, I>>(base?: I): QueryHasVKeyRequest {
    return QueryHasVKeyRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryHasVKeyRequest>, I>>(object: I): QueryHasVKeyRequest {
    const message = createBaseQueryHasVKeyRequest();
    message.name = object.name ?? "";
    return message;
  },
};

function createBaseQueryHasVKeyResponse(): QueryHasVKeyResponse {
  return { exists: false, id: 0n };
}

export const QueryHasVKeyResponse: MessageFns<QueryHasVKeyResponse> = {
  encode(message: QueryHasVKeyResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.exists !== false) {
      writer.uint32(8).bool(message.exists);
    }
    if (message.id !== 0n) {
      if (BigInt.asUintN(64, message.id) !== message.id) {
        throw new globalThis.Error("value provided for field message.id of type uint64 too large");
      }
      writer.uint32(16).uint64(message.id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryHasVKeyResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryHasVKeyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.exists = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.id = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryHasVKeyResponse {
    return {
      exists: isSet(object.exists) ? globalThis.Boolean(object.exists) : false,
      id: isSet(object.id) ? BigInt(object.id) : 0n,
    };
  },

  toJSON(message: QueryHasVKeyResponse): unknown {
    const obj: any = {};
    if (message.exists !== false) {
      obj.exists = message.exists;
    }
    if (message.id !== 0n) {
      obj.id = message.id.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryHasVKeyResponse>, I>>(base?: I): QueryHasVKeyResponse {
    return QueryHasVKeyResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryHasVKeyResponse>, I>>(object: I): QueryHasVKeyResponse {
    const message = createBaseQueryHasVKeyResponse();
    message.exists = object.exists ?? false;
    message.id = object.id ?? 0n;
    return message;
  },
};

function createBaseQueryNextVKeyIDRequest(): QueryNextVKeyIDRequest {
  return {};
}

export const QueryNextVKeyIDRequest: MessageFns<QueryNextVKeyIDRequest> = {
  encode(_: QueryNextVKeyIDRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryNextVKeyIDRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryNextVKeyIDRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): QueryNextVKeyIDRequest {
    return {};
  },

  toJSON(_: QueryNextVKeyIDRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryNextVKeyIDRequest>, I>>(base?: I): QueryNextVKeyIDRequest {
    return QueryNextVKeyIDRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryNextVKeyIDRequest>, I>>(_: I): QueryNextVKeyIDRequest {
    const message = createBaseQueryNextVKeyIDRequest();
    return message;
  },
};

function createBaseQueryNextVKeyIDResponse(): QueryNextVKeyIDResponse {
  return { nextId: 0n };
}

export const QueryNextVKeyIDResponse: MessageFns<QueryNextVKeyIDResponse> = {
  encode(message: QueryNextVKeyIDResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.nextId !== 0n) {
      if (BigInt.asUintN(64, message.nextId) !== message.nextId) {
        throw new globalThis.Error("value provided for field message.nextId of type uint64 too large");
      }
      writer.uint32(8).uint64(message.nextId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryNextVKeyIDResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryNextVKeyIDResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.nextId = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryNextVKeyIDResponse {
    return {
      nextId: isSet(object.nextId) ? BigInt(object.nextId) : isSet(object.next_id) ? BigInt(object.next_id) : 0n,
    };
  },

  toJSON(message: QueryNextVKeyIDResponse): unknown {
    const obj: any = {};
    if (message.nextId !== 0n) {
      obj.nextId = message.nextId.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryNextVKeyIDResponse>, I>>(base?: I): QueryNextVKeyIDResponse {
    return QueryNextVKeyIDResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryNextVKeyIDResponse>, I>>(object: I): QueryNextVKeyIDResponse {
    const message = createBaseQueryNextVKeyIDResponse();
    message.nextId = object.nextId ?? 0n;
    return message;
  },
};

function createBaseQueryParamsRequest(): QueryParamsRequest {
  return {};
}

export const QueryParamsRequest: MessageFns<QueryParamsRequest> = {
  encode(_: QueryParamsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryParamsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryParamsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): QueryParamsRequest {
    return {};
  },

  toJSON(_: QueryParamsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryParamsRequest>, I>>(base?: I): QueryParamsRequest {
    return QueryParamsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryParamsRequest>, I>>(_: I): QueryParamsRequest {
    const message = createBaseQueryParamsRequest();
    return message;
  },
};

function createBaseQueryParamsResponse(): QueryParamsResponse {
  return { params: undefined };
}

export const QueryParamsResponse: MessageFns<QueryParamsResponse> = {
  encode(message: QueryParamsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryParamsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseQueryParamsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.params = Params.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): QueryParamsResponse {
    return { params: isSet(object.params) ? Params.fromJSON(object.params) : undefined };
  },

  toJSON(message: QueryParamsResponse): unknown {
    const obj: any = {};
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryParamsResponse>, I>>(base?: I): QueryParamsResponse {
    return QueryParamsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryParamsResponse>, I>>(object: I): QueryParamsResponse {
    const message = createBaseQueryParamsResponse();
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    return message;
  },
};

/** Query provides defines the gRPC querier service. */
export interface Query {
  /** ProofVerify verifies a zk proof for email authentication. */
  ProofVerify(request: DeepPartial<QueryVerifyRequest>, metadata?: grpc.Metadata): Promise<ProofVerifyResponse>;
  /** VKey queries a verification key by ID */
  VKey(request: DeepPartial<QueryVKeyRequest>, metadata?: grpc.Metadata): Promise<QueryVKeyResponse>;
  /** VKeyByName queries a verification key by name */
  VKeyByName(request: DeepPartial<QueryVKeyByNameRequest>, metadata?: grpc.Metadata): Promise<QueryVKeyByNameResponse>;
  /** VKeys queries all verification keys with pagination */
  VKeys(request: DeepPartial<QueryVKeysRequest>, metadata?: grpc.Metadata): Promise<QueryVKeysResponse>;
  /** HasVKey checks if a verification key exists by name */
  HasVKey(request: DeepPartial<QueryHasVKeyRequest>, metadata?: grpc.Metadata): Promise<QueryHasVKeyResponse>;
  /** NextVKeyID queries the next available verification key ID */
  NextVKeyID(request: DeepPartial<QueryNextVKeyIDRequest>, metadata?: grpc.Metadata): Promise<QueryNextVKeyIDResponse>;
  /** Params returns zk module parameters. */
  Params(request: DeepPartial<QueryParamsRequest>, metadata?: grpc.Metadata): Promise<QueryParamsResponse>;
}

export class QueryClientImpl implements Query {
  private readonly rpc: Rpc;

  constructor(rpc: Rpc) {
    this.rpc = rpc;
    this.ProofVerify = this.ProofVerify.bind(this);
    this.VKey = this.VKey.bind(this);
    this.VKeyByName = this.VKeyByName.bind(this);
    this.VKeys = this.VKeys.bind(this);
    this.HasVKey = this.HasVKey.bind(this);
    this.NextVKeyID = this.NextVKeyID.bind(this);
    this.Params = this.Params.bind(this);
  }

  ProofVerify(request: DeepPartial<QueryVerifyRequest>, metadata?: grpc.Metadata): Promise<ProofVerifyResponse> {
    return this.rpc.unary(QueryProofVerifyDesc, QueryVerifyRequest.fromPartial(request), metadata);
  }

  VKey(request: DeepPartial<QueryVKeyRequest>, metadata?: grpc.Metadata): Promise<QueryVKeyResponse> {
    return this.rpc.unary(QueryVKeyDesc, QueryVKeyRequest.fromPartial(request), metadata);
  }

  VKeyByName(request: DeepPartial<QueryVKeyByNameRequest>, metadata?: grpc.Metadata): Promise<QueryVKeyByNameResponse> {
    return this.rpc.unary(QueryVKeyByNameDesc, QueryVKeyByNameRequest.fromPartial(request), metadata);
  }

  VKeys(request: DeepPartial<QueryVKeysRequest>, metadata?: grpc.Metadata): Promise<QueryVKeysResponse> {
    return this.rpc.unary(QueryVKeysDesc, QueryVKeysRequest.fromPartial(request), metadata);
  }

  HasVKey(request: DeepPartial<QueryHasVKeyRequest>, metadata?: grpc.Metadata): Promise<QueryHasVKeyResponse> {
    return this.rpc.unary(QueryHasVKeyDesc, QueryHasVKeyRequest.fromPartial(request), metadata);
  }

  NextVKeyID(request: DeepPartial<QueryNextVKeyIDRequest>, metadata?: grpc.Metadata): Promise<QueryNextVKeyIDResponse> {
    return this.rpc.unary(QueryNextVKeyIDDesc, QueryNextVKeyIDRequest.fromPartial(request), metadata);
  }

  Params(request: DeepPartial<QueryParamsRequest>, metadata?: grpc.Metadata): Promise<QueryParamsResponse> {
    return this.rpc.unary(QueryParamsDesc, QueryParamsRequest.fromPartial(request), metadata);
  }
}

export const QueryDesc = { serviceName: "xion.zk.v1.Query" };

export const QueryProofVerifyDesc: UnaryMethodDefinitionish = {
  methodName: "ProofVerify",
  service: QueryDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return QueryVerifyRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = ProofVerifyResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const QueryVKeyDesc: UnaryMethodDefinitionish = {
  methodName: "VKey",
  service: QueryDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return QueryVKeyRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = QueryVKeyResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const QueryVKeyByNameDesc: UnaryMethodDefinitionish = {
  methodName: "VKeyByName",
  service: QueryDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return QueryVKeyByNameRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = QueryVKeyByNameResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const QueryVKeysDesc: UnaryMethodDefinitionish = {
  methodName: "VKeys",
  service: QueryDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return QueryVKeysRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = QueryVKeysResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const QueryHasVKeyDesc: UnaryMethodDefinitionish = {
  methodName: "HasVKey",
  service: QueryDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return QueryHasVKeyRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = QueryHasVKeyResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const QueryNextVKeyIDDesc: UnaryMethodDefinitionish = {
  methodName: "NextVKeyID",
  service: QueryDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return QueryNextVKeyIDRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = QueryNextVKeyIDResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const QueryParamsDesc: UnaryMethodDefinitionish = {
  methodName: "Params",
  service: QueryDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return QueryParamsRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = QueryParamsResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export interface UnaryMethodDefinitionishR extends grpc.UnaryMethodDefinition<any, any> {
  requestStream: any;
  responseStream: any;
}

type UnaryMethodDefinitionish = UnaryMethodDefinitionishR;

interface Rpc {
  unary<T extends UnaryMethodDefinitionish>(
    methodDesc: T,
    request: any,
    metadata: grpc.Metadata | undefined,
  ): Promise<any>;
}

export class GrpcWebImpl {
  private host: string;
  private options: {
    transport?: grpc.TransportFactory;

    debug?: boolean;
    metadata?: grpc.Metadata;
    upStreamRetryCodes?: number[];
  };

  constructor(
    host: string,
    options: {
      transport?: grpc.TransportFactory;

      debug?: boolean;
      metadata?: grpc.Metadata;
      upStreamRetryCodes?: number[];
    },
  ) {
    this.host = host;
    this.options = options;
  }

  unary<T extends UnaryMethodDefinitionish>(
    methodDesc: T,
    _request: any,
    metadata: grpc.Metadata | undefined,
  ): Promise<any> {
    const request = { ..._request, ...methodDesc.requestType };
    const maybeCombinedMetadata = metadata && this.options.metadata
      ? new BrowserHeaders({ ...this.options?.metadata.headersMap, ...metadata?.headersMap })
      : metadata ?? this.options.metadata;
    return new Promise((resolve, reject) => {
      grpc.unary(methodDesc, {
        request,
        host: this.host,
        metadata: maybeCombinedMetadata ?? {},
        ...(this.options.transport !== undefined ? { transport: this.options.transport } : {}),
        debug: this.options.debug ?? false,
        onEnd: function (response) {
          if (response.status === grpc.Code.OK) {
            resolve(response.message!.toObject());
          } else {
            const err = new GrpcWebError(response.statusMessage, response.status, response.trailers);
            reject(err);
          }
        },
      });
    });
  }
}

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export class GrpcWebError extends globalThis.Error {
  constructor(message: string, public code: grpc.Code, public metadata: grpc.Metadata) {
    super(message);
  }
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
